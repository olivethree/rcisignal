% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_data.R
\name{simulate_briefrc_data}
\alias{simulate_briefrc_data}
\title{Simulate Brief-RC reverse-correlation data}
\usage{
simulate_briefrc_data(
  n_per_condition = 50L,
  conditions = c("target", "control"),
  n_trials = NULL,
  images_per_trial = 12L,
  noise_pool_size = NULL,
  img_size = 256L,
  base_image = NULL,
  signal_strength = "weak",
  signal_region = "eyes",
  rt_contamination_fast = 0.02,
  rt_contamination_slow = 0.02,
  noise_type = "sinusoid",
  nscales = 5L,
  sigma = 25,
  rdata_dir = NULL,
  seed = NULL,
  progress = TRUE
)
}
\arguments{
\item{n_per_condition, conditions, img_size, base_image, signal_strength, signal_region, rt_contamination_fast, rt_contamination_slow, noise_type, nscales, sigma, rdata_dir, seed, progress}{See \code{\link[=simulate_2ifc_data]{simulate_2ifc_data()}}. For Brief-RC the rcicr-format
\code{.Rdata} is informational (downstream Brief-RC functions read
\verb{$noise_matrix} directly); it is written for symmetry with the
2IFC path and as a portable on-disk artefact when
\code{rdata_dir} is non-\code{NULL} (stable filename
\code{rcisignal_sim_briefrc_stimuli.Rdata}).}

\item{n_trials}{Integer or \code{NULL}. Brief-RC trials per
participant. When \code{NULL} (default), it is derived from the
pair budget as \code{noise_pool_size \%/\% (images_per_trial / 2)},
so the simulation matches \code{simulate_2ifc_data()} on number of
unique noise pairs rather than on trial count. With the
defaults (\code{noise_pool_size = 500}, \code{images_per_trial = 12})
this resolves to \code{n_trials = 83}; with \code{images_per_trial = 20} it resolves to \code{50}. Supplying \code{n_trials} directly
overrides this and (if \code{noise_pool_size} is also \code{NULL})
restores the older "pool grows with trials" behavior.}

\item{images_per_trial}{Integer (even). Number of images shown
per trial; half are original and half are inverted versions of
the same noise patterns. Default \code{12} (= 6 pairs).}

\item{noise_pool_size}{Integer or \code{NULL}. Total number of noise
patterns to pre-generate. When \code{NULL} and \code{n_trials} is also
\code{NULL}, defaults to \code{500} (matched image-pair budget against
the 2IFC default of 500 trials x 1 pair). When \code{NULL} but
\code{n_trials} is supplied, defaults to \code{n_trials * (images_per_trial / 2)} so within-participant sampling stays
without replacement. Pass a larger value than that to study
sub-sampling.}
}
\value{
An object of class \code{"rcisignal_sim"}. See
\code{\link[=simulate_2ifc_data]{simulate_2ifc_data()}} for the structure. The \code{meta} list also
carries \code{images_per_trial} and \code{noise_pool_size}.
}
\description{
Generates a synthetic Brief-RC dataset (Schmitz, Rougier, &
Yzerbyt, 2024) where each trial shows several original/inverted
noise pairs and the participant picks one image. Output is
shape-compatible with \code{\link[=run_diagnostics]{run_diagnostics()}},
\code{\link[=ci_from_responses_briefrc]{ci_from_responses_briefrc()}}, and the reliability /
discriminability functions.

Noise pool is generated once via \code{\link[rcicr:generateNoisePattern]{rcicr::generateNoisePattern()}}
and \code{\link[rcicr:generateNoiseImage]{rcicr::generateNoiseImage()}} and then sampled per trial
(without replacement within a participant; the same pool is
shared across participants).
}
\section{Signal model}{

Per trial, with \verb{images_per_trial = 2k}, the participant sees \code{k}
original/inverted pairs. Each of the \verb{2k} images has utility
\code{beta * (noise \%*\% s) / scale + Gumbel(0, 1)}, where \code{noise} is
\code{+noise[, j]} for the original version and \code{-noise[, j]} for the
inverted version of pair \code{j}. The participant picks the image
with the highest utility (multinomial logit / softmax). The
recorded \code{stimulus} is the pool index of the chosen pair;
\code{response} is \code{+1} if the original version was chosen and \code{-1}
for the inverted version.
}

\section{RT model}{

Shifted lognormal: \code{rt = round(exp(rnorm(n, log(800), 0.5)) + 150)}, in ms. A small fraction of fast (<200 ms) and slow
(>5000 ms) contaminants are mixed in (default 2\% each) so the
diagnostic functions (\code{\link[=check_rt]{check_rt()}}) have something to flag.
}

\examples{
\dontrun{
# `sim$data` is a plain data frame (columns: participant_id, stimulus,
# response, condition, rt) — same shape ci_from_responses_briefrc() and
# the check_*() functions expect from your own CSV.
# `sim$noise_matrix` is a numeric matrix `n_pixels x pool_size` — same
# shape read_noise_matrix() returns from your Brief-RC OSF txt file.
sim <- simulate_briefrc_data(n_per_condition = 10, n_trials = 60, seed = 1)
run_diagnostics(sim$data, method = "briefrc",
                noise_matrix = sim$noise_matrix, col_rt = "rt")
cis <- ci_from_responses_briefrc(sim$data,
                                 noise_matrix = sim$noise_matrix)
run_reliability(cis$signal_matrix, n_permutations = 200L, seed = 1)
}
}
\references{
Schmitz, M., Rougier, M., & Yzerbyt, V. (2024). Introducing the
brief reverse correlation: an improved tool to assess visual
representations. \emph{European Journal of Social Psychology}.
\doi{10.1002/ejsp.3100}
}
\seealso{
\code{\link[=simulate_2ifc_data]{simulate_2ifc_data()}}, \code{\link[=run_diagnostics]{run_diagnostics()}},
\code{\link[=ci_from_responses_briefrc]{ci_from_responses_briefrc()}}.
}
