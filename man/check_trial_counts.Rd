% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_trial_counts.R
\name{check_trial_counts}
\alias{check_trial_counts}
\title{Check trial counts per participant}
\usage{
check_trial_counts(
  responses,
  expected_n = NULL,
  method = c("2ifc", "briefrc"),
  col_participant = "participant_id",
  ...
)
}
\arguments{
\item{responses}{A data frame of trial-level responses.}

\item{expected_n}{Optional. A single integer (all participants should
have this many trials) or a vector of allowed counts (useful for
designs with multiple trial-count conditions, e.g. \code{c(300, 500, 1000)}).
If \code{NULL}, no flagging occurs.}

\item{method}{Either \code{"2ifc"} or \code{"briefrc"}. Currently informational.}

\item{col_participant}{Name of the participant id column.}

\item{...}{Unused. Reserved for consistency with other check functions.}
}
\value{
An \code{\link[=rcisignal_diag_result]{rcisignal_diag_result()}} object. The \code{data} element contains
\code{per_participant}, a data frame with one row per participant and
their trial count.
}
\description{
Counts the number of rows per participant and compares against expected
trial counts. If \code{expected_n} is not supplied, the check reports the
observed distribution without flagging anything. If \code{expected_n} is
supplied, participants whose trial count is not in the allowed set are
flagged.
}
\examples{
responses <- data.frame(
  participant_id = rep(1:3, times = c(300, 300, 250)),
  stimulus       = 1,
  response       = 1
)
check_trial_counts(responses, expected_n = 300)

}
