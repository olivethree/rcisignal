% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_infoval.R
\name{diagnose_infoval}
\alias{diagnose_infoval}
\title{Guided diagnostic for low or negative infoVal}
\usage{
diagnose_infoval(
  responses,
  method = NULL,
  rdata = NULL,
  stimuli = NULL,
  noise_matrix = NULL,
  baseimage = "base",
  col_participant = "participant_id",
  col_stimulus = "stimulus",
  col_response = "response",
  iter = 1000L,
  face_mask = "auto",
  with_replacement = "auto",
  seed = NULL,
  progress = TRUE,
  ...
)
}
\arguments{
\item{responses}{Data frame with one row per trial. Required
columns: \code{participant_id}, \code{stimulus}, \code{response} (values in
\verb{\{-1, +1\}}). Load yours from CSV via \code{\link[=read_responses]{read_responses()}} or
\code{\link[utils:read.table]{utils::read.csv()}}; column names are configurable via the
\verb{col_*} arguments.}

\item{method}{\code{"2ifc"} or \code{"briefrc"}. If \code{NULL}, inferred from
whichever of \code{rdata} / \code{noise_matrix} is supplied.}

\item{rdata}{Path to an rcicr \code{.RData} file (2IFC). Either
\code{rdata} or \code{stimuli} must be supplied for the 2IFC path.}

\item{stimuli}{In-memory stimuli list (the \verb{$stimuli} element
of an \code{rcisignal_sim} object). Use in place of \code{rdata} when
the file path no longer resolves (e.g. after \code{\link[=saveRDS]{saveRDS()}}/
\code{\link[=readRDS]{readRDS()}} across R sessions).}

\item{noise_matrix}{Path to a Brief-RC noise-matrix text file, or
an already-loaded numeric matrix.}

\item{baseimage}{Name of the base image in the rdata
\code{base_face_files} list. Default \code{"base"}. Only consulted for 2IFC.}

\item{col_participant, col_stimulus, col_response}{Column names.}

\item{iter}{Reference-distribution Monte Carlo size. Default
\code{1000L} (a diagnostic-grade value). Bump to \code{10000} for
publication numbers.}

\item{face_mask}{Mask specification. One of:
\itemize{
\item \code{"auto"} (default): generate a Schmitz 2024 oval via
\code{\link[=make_face_mask]{make_face_mask()}} sized to the image dims.
\item \code{NULL}: skip the masked-vs-unmasked comparison.
\item A logical vector of length \code{n_pixels}.
\item A numeric matrix matching the image dims (coerced via \verb{> 0.5}).
\item A character path to a PNG / JPEG mask image (loaded via
\code{\link[=read_face_mask]{read_face_mask()}}).
}}

\item{with_replacement}{Sampling regime forwarded to \code{\link[=infoval]{infoval()}} for
building the reference distribution at the across-trials level.
\code{"auto"} (default) matches the standard Brief-RC convention
(without replacement when a producer's trial count fits in the
pool, with replacement otherwise). Set explicitly only if your
task design departs from this convention. See \code{\link[=infoval]{infoval()}} for
details. Ignored on the 2IFC path because there \code{n_trials}
equals the pool size by construction.}

\item{seed}{Optional integer; RNG state restored on exit.}

\item{progress}{Show a \code{cli} progress bar.}

\item{...}{Unused.}
}
\value{
An \code{\link[=rcisignal_diag_result]{rcisignal_diag_result()}}. \code{data} carries the rich output:
\itemize{
\item \code{random_responder_z} -- numeric scalar.
\item \code{infoval_unmasked} -- \code{rcisignal_diag_infoval} object (see \code{\link[=infoval]{infoval()}}).
\item \code{infoval_masked} -- \code{rcisignal_diag_infoval}; \code{NULL} when no mask.
\item \code{group_mean_z_unmasked}, \code{group_mean_z_masked} -- scalars.
\item \code{tally} -- named integer vector with counts per z band.
\item \code{mask} -- logical vector or \code{NULL}.
\item \code{interpretation} -- character vector of human-readable bullets.
}
}
\description{
Walks through six checks that explain why per-producer infoVal often
looks "low" even on healthy data, and surfaces the numbers that
actually belong in a methods section. Use this when individual
producers' infoVal z-scores look suspiciously low or negative and
you want to know whether the data is broken or merely structurally
diluted.
}
\details{
The six steps:
\enumerate{
\item Simulate the reference distribution at every unique producer
trial count present in the data.
\item Sanity-check the reference with a simulated random responder.
A random producer should land near \code{z = 0}; \verb{|z| > 2} indicates
the reference is mis-calibrated.
\item Compute per-producer z unmasked.
\item Compute per-producer z with the supplied (or auto-generated)
face mask, and report the median masked-vs-unmasked z lift.
\item Compute the group-mean CI's z, against a reference matched to
the same producer count and trial counts.
\item Tabulate per-producer z into four bands (\verb{< -1.96},
\verb{[-1.96, 0)}, \verb{[0, 1.96)}, \verb{>= 1.96}) and produce interpretation
bullets summarising the evidence.
}

Whether per-producer z values cluster above or below the
conventional 1.96 threshold is paradigm- and target-dependent.
Brinkman et al. (2019) report 68\% (lab) and 54\% (online) of 2IFC
participants clearing 1.96 on perceived gender, with mean
per-participant infoVal 3.9 (lab) and 2.9 (online); Schmitz et al.
(2024) report Brief-RC infoVals systematically below 1.96 across
all conditions in both experiments. The reportable summary is
therefore context-specific: per-producer z is useful for exclusion
decisions on individual cases. Neither paper computes a group-mean
CI's z directly, but the mathematical extension (a group-mean CI
compared against an N-producer-matched reference) is a useful
aggregate when individual-level z is noisy, and \code{diagnose_infoval()}
reports it.

For \code{method = "2ifc"}, the function calls \code{rcicr} to reconstruct
per-trial noise patterns from \code{stimuli_params} and \code{p} (the
\code{.RData} does not store the patterns themselves; see vignette
"What data the package expects"). The reconstruction is one
\code{rcicr::generateNoiseImage()} call per pool item, which is fast
(seconds) but does require \code{rcicr} to be installed. The Brief-RC
path needs no \code{rcicr} dependency.
}
\examples{
\dontrun{
sim <- simulate_2ifc_data(n_per_condition = 10, n_trials = 60, seed = 1)
diagnose_infoval(sim$data, method = "2ifc",
                 rdata = sim$rdata_path, iter = 200L)

# Same call via the session-portable in-memory stimuli list:
diagnose_infoval(sim$data, method = "2ifc",
                 stimuli = sim$stimuli, iter = 200L)
}

}
\references{
Brinkman, L., Goffin, S., van de Schoot, R., van Haren, N. E. M.,
Dotsch, R., & Aarts, H. (2019). Quantifying the informational value
of classification images. \emph{Behavior Research Methods}, 51(5),
2059-2073.

Schmitz, M., Rougier, M., & Yzerbyt, V. (2024). Introducing the
brief reverse correlation: an improved tool to assess visual
representations. \emph{European Journal of Social Psychology}.
}
\seealso{
\code{\link[=infoval]{infoval()}}, \code{\link[=make_face_mask]{make_face_mask()}}, \code{\link[=compute_infoval_summary]{compute_infoval_summary()}}.
}
