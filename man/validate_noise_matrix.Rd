% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_noise_matrix.R
\name{validate_noise_matrix}
\alias{validate_noise_matrix}
\title{Validate a noise matrix}
\usage{
validate_noise_matrix(mat, expected_pixels = NULL, expected_stimuli = NULL)
}
\arguments{
\item{mat}{A numeric matrix, typically from \code{\link[=read_noise_matrix]{read_noise_matrix()}}.}

\item{expected_pixels}{Optional integer. If supplied, checks that
\code{nrow(mat) == expected_pixels}. For example, 128*128 = 16384.}

\item{expected_stimuli}{Optional integer. If supplied, checks
that \code{ncol(mat) == expected_stimuli}.}
}
\value{
An object of class \code{"rcisignal_diag_result"}.
}
\description{
Runs basic sanity checks on a noise matrix: numeric, finite,
expected dimensions. Returns an \code{\link[=rcisignal_diag_result]{rcisignal_diag_result()}} with
status \code{"pass"}, \code{"warn"}, or \code{"fail"} rather than aborting,
because users typically want to see all problems at once, not
just the first.
}
\examples{
mat <- matrix(rnorm(16384 * 10, sd = 0.05),
              nrow = 16384, ncol = 10)
validate_noise_matrix(mat,
                      expected_pixels  = 16384,
                      expected_stimuli = 10)
}
\seealso{
\code{\link[=read_noise_matrix]{read_noise_matrix()}}, \code{\link[=rcisignal_diag_result]{rcisignal_diag_result()}}.
}
