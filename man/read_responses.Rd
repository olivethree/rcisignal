% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_responses.R
\name{read_responses}
\alias{read_responses}
\title{Read reverse-correlation response data from a CSV file}
\usage{
read_responses(
  path,
  method = c("2ifc", "briefrc"),
  col_participant = "participant_id",
  col_stimulus = "stimulus",
  col_response = "response",
  col_rt = NULL
)
}
\arguments{
\item{path}{Path to a CSV file. Any format readable by
\code{\link[data.table:fread]{data.table::fread()}} is accepted (comma-, tab-, or
space-delimited).}

\item{method}{Either \code{"2ifc"} or \code{"briefrc"}. Currently
informational only; downstream checks use this to select
method-specific logic.}

\item{col_participant, col_stimulus, col_response}{Column names that
must exist in the file. Default to the package-wide standard
names.}

\item{col_rt}{Optional column name for response time. If
provided, must exist in the file.}
}
\value{
A \link[data.table:data.table]{data.table::data.table} with all columns from the CSV.
}
\description{
Reads a trial-level response CSV and returns a
\link[data.table:data.table]{data.table::data.table}. Validates that the required columns are
present but does not coerce types or rename columns. Use this to
load responses for the diagnostics battery
(\code{\link[=run_diagnostics]{run_diagnostics()}} / \verb{check_*()}) or as input to
\code{\link[=ci_from_responses_2ifc]{ci_from_responses_2ifc()}} / \code{\link[=ci_from_responses_briefrc]{ci_from_responses_briefrc()}}.
}
\examples{
\dontrun{
responses <- read_responses("my_data.csv", method = "2ifc")
}
}
\seealso{
\code{\link[=run_diagnostics]{run_diagnostics()}}, \code{\link[=ci_from_responses_2ifc]{ci_from_responses_2ifc()}},
\code{\link[=ci_from_responses_briefrc]{ci_from_responses_briefrc()}}
}
