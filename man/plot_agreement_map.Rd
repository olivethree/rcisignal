% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agreement_map.R
\name{plot_agreement_map}
\alias{plot_agreement_map}
\title{Per-pixel agreement heatmap for a producer signal matrix}
\usage{
plot_agreement_map(
  signal_matrix,
  img_dims = NULL,
  mask = NULL,
  threshold = NULL,
  zlim = NULL,
  palette = c("diverging", "fire"),
  base_image = NULL,
  alpha_max = 0.7,
  main = "Per-pixel producer agreement (t-map)",
  ...
)
}
\arguments{
\item{signal_matrix}{Pixels x participants raw mask (as returned
by \verb{ci_from_responses_*()} or \code{read_cis()} + \code{extract_signal()}).}

\item{img_dims}{Integer \code{c(nrow, ncol)}. If \code{NULL}, inferred from
\code{attr(signal_matrix, "img_dims")} or from \code{sqrt(n_pixels)} if
the latter is a whole number.}

\item{mask}{Optional logical vector of length
\code{nrow(signal_matrix)} (column-major) restricting display to a
region (e.g., \code{make_face_mask(img_dims, region = "eyes")}).
Also accepts the output of \code{\link[=read_face_mask]{read_face_mask()}} for PNG/JPEG
masks. Pixels outside the mask render as \code{NA} (transparent).}

\item{threshold}{Optional positive numeric. When supplied,
pixels with \verb{|t| < threshold} are rendered in the neutral
(white) colour, making clusters of agreement stand out.
Default \code{NULL} (full continuous map).}

\item{zlim}{Numeric \code{c(low, high)} for the colour scale. For
\code{palette = "diverging"} (default), defaults to
\verb{c(-max(|t|), max(|t|))} so the neutral colour aligns with
\code{t = 0}. For \code{palette = "fire"}, defaults to \verb{c(0, max(|t|))}
so pale yellow aligns with \verb{|t| = 0}.}

\item{palette}{Character. \code{"diverging"} (default; positive =
blue, negative = red, neutral = white) encodes sign in hue and
magnitude in saturation. \code{"fire"} encodes \verb{|t|} only on a
single-hue ramp (pale yellow at zero -> deep red at large
\verb{|t|}); use this when the question is "where do producers have
a consistent opinion" and direction is not needed. The \code{"fire"}
view discards sign; pair with \code{palette = "diverging"} or with
\code{\link[=plot_ci_overlay]{plot_ci_overlay()}} to recover direction at a region of
interest.}

\item{base_image}{Optional. Either a numeric matrix
(\verb{nrow x ncol}, grayscale, values in 0-1) or a path to a
PNG/JPEG file. When supplied, the t-map is composited on top of
the grayscale base; out-of-mask and subthreshold pixels render
fully transparent. When \code{NULL} (default), the map is drawn on a
flat panel via \code{graphics::image()} (the historical behaviour).}

\item{alpha_max}{Numeric in \verb{[0, 1]}. Maximum opacity of the
heatmap at the colour-scale top (\code{zlim_max}) when \code{base_image}
is supplied. Ignored otherwise. Default 0.7.}

\item{main}{Title.}

\item{...}{Passed to \code{graphics::image()}.}
}
\value{
Invisibly, a list with \code{t_map} (numeric vector of t values
per pixel; always signed regardless of palette), \code{n} (producer
count), \code{img_dims}, \code{mask} (if supplied), \code{zlim} (the colour
scale used), and \code{palette} (the palette name).
}
\description{
Visualises where producers in a single condition agree on the
direction of signal. For each pixel, computes a one-sample
t-statistic against zero across producers
(\code{mean / (sd / sqrt(N))}), then displays the resulting map with
a diverging colour palette (positive = agreement on positive
signal, negative = agreement on negative signal, zero = no
agreement). Saturation of the colour is the magnitude of the
agreement, \emph{not} the value of the group-mean CI.

Use this to answer "where do producers consistently \emph{agree} the
target trait lives in the face?". Pair with the group-mean CI
image (raw mask or rendered) to see \emph{direction} and \emph{agreement}
side by side.
}
\details{
This is structurally a one-sample t-map (vs zero); pixels where
producers' contributions are large \emph{and consistent in sign} get
high |t|, pixels where contributions are random get t near zero.
Cluster-permutation inference would normally accompany this for
formal pixel-level FWER control between conditions
(\code{\link[=rel_cluster_test]{rel_cluster_test()}}); the agreement map is the \strong{descriptive}
counterpart for a single condition.
}
\section{Reading the plot}{

Two palettes are available; pick by what question you are
asking the data.

\strong{\code{palette = "diverging"} (default).} Encodes sign and
magnitude together. Both deep red and deep blue indicate
\strong{strong} agreement among producers; only the \strong{direction}
differs. "No agreement" is the neutral colour (white), not red.
\itemize{
\item \strong{Hue} encodes the sign of the per-pixel one-sample \code{t}.
Blue = producers consistently \emph{add} to the base at that pixel
(positive agreement, producers chose noise that lightens the
region); red = consistently \emph{subtract} (negative agreement,
producers chose noise that darkens the region).
\item \strong{Saturation} encodes \verb{|t|}. Deep colour at either end means
strong, consistent agreement; pale colour means weak or
inconsistent. The colourbar on the right reads in \code{t} units.
\item \strong{\code{zlim}} is symmetric around zero by default so the neutral
colour aligns with \code{t = 0}. Pass \code{zlim = c(-z, z)} to fix the
scale across panels for direct comparison.
}

\strong{\code{palette = "fire"}.} Encodes \verb{|t|} only on a single-hue ramp
(pale yellow at zero -> deep red at large \verb{|t|}). Use when the
question is \emph{where} producers have a consistent opinion and the
direction is not needed. The \code{"fire"} view \strong{discards sign by
design}; it cannot distinguish "producers consistently added"
from "producers consistently subtracted". To recover direction at
any region of interest, view the same data with
\code{palette = "diverging"} or pair with \code{\link[=plot_ci_overlay]{plot_ci_overlay()}} of the
group-mean CI.
\itemize{
\item \strong{Hue intensity} encodes \verb{|t|}. Pale yellow / near-white at
low \verb{|t|} (so the underlying base face shows through low-
agreement regions); orange at moderate \verb{|t|}; deep red at
large \verb{|t|}. The colourbar reads in \verb{|t|} units.
\item \strong{\code{zlim}} defaults to \verb{c(0, max(|t|))} and is asymmetric.
}

\strong{Common to both palettes.}
\itemize{
\item \strong{\code{threshold}} clips colour to the neutral end below
\verb{|t| < threshold}, making strong-agreement clusters stand out.
This is descriptive only; it does not provide FWER control. For
inferential pixel significance, use \code{\link[=agreement_map_test]{agreement_map_test()}} and
render its result directly via
\code{plot(agreement_map_test(...))}, or overlay the contours via
\code{\link[=plot_ci_overlay]{plot_ci_overlay()}}.
\item \strong{\code{base_image}} composites the heatmap on top of a grayscale
base face so anatomical context shows through. Out-of-mask and
subthreshold pixels render fully transparent; the per-pixel
opacity scales \verb{|t| / zlim_max} up to \code{alpha_max}. Works for
both palettes. The colour bar still shows the full scale so
magnitudes are readable off the rendered overlay.
\item The diverging colour convention (blue = positive,
red = negative) matches \code{\link[=plot_ci_overlay]{plot_ci_overlay()}} and the
cluster-test plots so the same group CI reads consistently
across the package. The \code{"fire"} option is unique to this
function; the CI-overlay and cluster-test plots need to show
direction, so they do not provide a magnitude-only view.
}
}

\examples{
\dontrun{
# Minimal call-signature demo with a synthetic input. The agreement
# map will look flat because the input is pure noise.
n_side <- 32L
n_pix  <- n_side * n_side
set.seed(1)
signal_matrix <- matrix(rnorm(n_pix * 20L), n_pix, 20L)
plot_agreement_map(signal_matrix, img_dims = c(n_side, n_side))
}

\dontrun{
# Same function, richer input: simulate Brief-RC responses with a
# signal planted in the eye region, then look at the agreement map.
# Producers should consistently agree on the planted region.
sim <- simulate_briefrc_data(
  n_per_condition = 20, n_trials = 60, conditions = "target",
  signal_region = "eyes", signal_strength = "strong", seed = 1
)
cis <- ci_from_responses_briefrc(sim$data, noise_matrix = sim$noise_matrix)
plot_agreement_map(cis$signal_matrix)
}

\dontrun{
# Composite the agreement map on the base face for a single
# publication-grade figure. Works for both palettes; the
# "diverging" branch matches plot_ci_overlay()'s colour mapping.
sim <- simulate_briefrc_data(
  n_per_condition = 20, n_trials = 60, conditions = "target",
  signal_region = "eyes", signal_strength = "strong", seed = 1
)
cis <- ci_from_responses_briefrc(sim$data, noise_matrix = sim$noise_matrix)
plot_agreement_map(cis$signal_matrix,
                   base_image = sim$base_face,
                   threshold  = 2.0,
                   main       = "Agreement t-map over base face")
}
}
\seealso{
\code{\link[=plot_ci_overlay]{plot_ci_overlay()}} for the producer-mean counterpart
(signed CI, optionally with FWE contours); \code{\link[=agreement_map_test]{agreement_map_test()}}
for FWE-controlled significance, and its \code{plot()} method for a
one-call agreement map with contours; \code{\link[=rel_cluster_test]{rel_cluster_test()}} for
inferential between-condition tests; \code{\link[=make_face_mask]{make_face_mask()}} /
\code{\link[=read_face_mask]{read_face_mask()}} for the optional \code{mask}.
}
