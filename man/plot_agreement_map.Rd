% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agreement_map.R
\name{plot_agreement_map}
\alias{plot_agreement_map}
\title{Per-pixel agreement heatmap for a producer signal matrix}
\usage{
plot_agreement_map(
  signal_matrix,
  img_dims = NULL,
  mask = NULL,
  threshold = NULL,
  zlim = NULL,
  palette = c("diverging", "viridis"),
  main = "Per-pixel producer agreement (t-map)",
  ...
)
}
\arguments{
\item{signal_matrix}{Pixels x participants raw mask (as returned
by \verb{ci_from_responses_*()} or \code{read_cis()} + \code{extract_signal()}).}

\item{img_dims}{Integer \code{c(nrow, ncol)}. If \code{NULL}, inferred from
\code{attr(signal_matrix, "img_dims")} or from \code{sqrt(n_pixels)} if
the latter is a whole number.}

\item{mask}{Optional logical vector of length
\code{nrow(signal_matrix)} (column-major) restricting display to a
region (e.g., \code{make_face_mask(img_dims, region = "eyes")}).
Also accepts the output of \code{\link[=read_face_mask]{read_face_mask()}} for PNG/JPEG
masks. Pixels outside the mask render as \code{NA} (transparent).}

\item{threshold}{Optional positive numeric. When supplied,
pixels with \verb{|t| < threshold} are rendered in the neutral
(white) colour, making clusters of agreement stand out.
Default \code{NULL} (full continuous map).}

\item{zlim}{Numeric \code{c(low, high)} for the colour scale. Default
is symmetric around zero at \verb{c(-max(|t|), max(|t|))} so the
neutral colour aligns with t = 0.}

\item{palette}{Character. \code{"diverging"} (default; positive =
blue, negative = red, neutral = white) or \code{"viridis"} (no
neutral; for absolute-magnitude views).}

\item{main}{Title.}

\item{...}{Passed to \code{graphics::image()}.}
}
\value{
Invisibly, a list with \code{t_map} (numeric vector of t values
per pixel), \code{n} (producer count), \code{img_dims}, and \code{mask} (if
supplied) — useful for further analysis or replotting.
}
\description{
Visualises where producers in a single condition agree on the
direction of signal. For each pixel, computes a one-sample
t-statistic against zero across producers
(\code{mean / (sd / sqrt(N))}), then displays the resulting map with
a diverging colour palette (positive = agreement on positive
signal, negative = agreement on negative signal, zero = no
agreement). Saturation of the colour is the magnitude of the
agreement, \emph{not} the value of the group-mean CI.

Use this to answer "where do producers consistently \emph{agree} the
target trait lives in the face?". Pair with the group-mean CI
image (raw mask or rendered) to see \emph{direction} and \emph{agreement}
side by side.
}
\details{
This is structurally a one-sample t-map (vs zero); pixels where
producers' contributions are large \emph{and consistent in sign} get
high |t|, pixels where contributions are random get t near zero.
Cluster-permutation inference would normally accompany this for
formal pixel-level FWER control between conditions
(\code{\link[=rel_cluster_test]{rel_cluster_test()}}); the agreement map is the \strong{descriptive}
counterpart for a single condition.
}
\section{Reading the plot}{

\itemize{
\item \strong{Colour} encodes the sign of the per-pixel one-sample t.
Blue = producers consistently \emph{add} to the base at that pixel
(positive agreement); red = consistently \emph{subtract} (negative
agreement); white = no agreement (t near zero).
\item \strong{Saturation} encodes \verb{|t|}: deeper colour means the agreement
among producers is both large and consistent in sign. The
colourbar on the right reads in \code{t} units (one-sample vs 0).
\item \strong{\code{zlim}} is symmetric around zero by default so the neutral
colour aligns with \code{t = 0}. Pass \code{zlim = c(-z, z)} to fix the
scale across panels for direct comparison.
\item \strong{\code{threshold}} clips colour to white below \verb{|t| < threshold},
making strong-agreement clusters stand out. This is descriptive
only; it does not provide FWER control. For inferential pixel
significance, use \code{\link[=agreement_map_test]{agreement_map_test()}} and overlay the
contours via \code{\link[=plot_ci_overlay]{plot_ci_overlay()}}.
\item Colour convention matches \code{\link[=plot_ci_overlay]{plot_ci_overlay()}} and the
cluster-test plots so the same group CI reads consistently
across the package.
}
}

\examples{
\dontrun{
# Minimal call-signature demo with a synthetic input. The agreement
# map will look flat because the input is pure noise.
n_side <- 32L
n_pix  <- n_side * n_side
set.seed(1)
signal_matrix <- matrix(rnorm(n_pix * 20L), n_pix, 20L)
plot_agreement_map(signal_matrix, img_dims = c(n_side, n_side))
}

\dontrun{
# Same function, richer input: simulate Brief-RC responses with a
# signal planted in the eye region, then look at the agreement map.
# Producers should consistently agree on the planted region.
sim <- simulate_briefrc_data(
  n_per_condition = 20, n_trials = 60, conditions = "target",
  signal_region = "eyes", signal_strength = "strong", seed = 1
)
cis <- ci_from_responses_briefrc(sim$data, noise_matrix = sim$noise_matrix)
plot_agreement_map(cis$signal_matrix)
}
}
\seealso{
\code{\link[=make_face_mask]{make_face_mask()}}, \code{\link[=rel_cluster_test]{rel_cluster_test()}} for inferential
between-condition tests.
}
