% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_discriminability.R
\name{run_discriminability}
\alias{run_discriminability}
\title{Run every between-condition discriminability metric}
\usage{
run_discriminability(
  signal_matrix_a,
  signal_matrix_b,
  img_dims = NULL,
  n_permutations = 2000L,
  n_boot = 2000L,
  cluster_threshold = 2,
  alpha = 0.05,
  ci_level = 0.95,
  mask = NULL,
  seed = NULL,
  progress = TRUE,
  acknowledge_scaling = FALSE
)
}
\arguments{
\item{signal_matrix_a, signal_matrix_b}{Pixels x participants,
base-subtracted. Row counts must match.}

\item{img_dims}{Integer \code{c(nrow, ncol)}. If \code{NULL}, inferred
from \code{attr(signal_matrix_a, "img_dims")}.}

\item{n_permutations}{Passed to \code{\link[=rel_cluster_test]{rel_cluster_test()}}. Default
2000.}

\item{n_boot}{Passed to \code{\link[=rel_dissimilarity]{rel_dissimilarity()}}. Default 2000.}

\item{cluster_threshold}{Passed to \code{\link[=rel_cluster_test]{rel_cluster_test()}}.
Default 2.0.}

\item{alpha}{Passed to \code{\link[=rel_cluster_test]{rel_cluster_test()}}. Default 0.05.}

\item{ci_level}{Passed to \code{\link[=rel_dissimilarity]{rel_dissimilarity()}}. Default 0.95.}

\item{mask}{Optional logical vector of length
\code{nrow(signal_matrix_a)} (column-major) threaded through to both
downstream calls. Build with \code{\link[=make_face_mask]{make_face_mask()}} (parametric
oval and sub-regions) or \code{\link[=read_face_mask]{read_face_mask()}} (PNG/JPEG mask).}

\item{seed}{Optional integer.}

\item{progress}{Show \code{cli} progress bars.}

\item{acknowledge_scaling}{Logical. Forwarded.}
}
\value{
Object of class \code{rcisignal_rel_report} with \verb{$results}
= named list of two result objects (\code{cluster_test},
\code{dissimilarity}) and \verb{$method = "discriminability"}.
}
\description{
Convenience orchestrator that runs \code{\link[=rel_cluster_test]{rel_cluster_test()}} and
\code{\link[=rel_dissimilarity]{rel_dissimilarity()}} on two condition signal matrices and
wraps both results in an \code{rcisignal_rel_report} for joint
printing / plotting.

Use this when you want both the spatial-pattern test and the
overall magnitude test in one call.
}
\section{Reading the result}{

\verb{$results$cluster_test} and \verb{$results$dissimilarity}, one
result object each, fields as in the standalone functions.
\verb{$method = "discriminability"}.
}

\section{Reading the plot}{

Calling \code{plot(result)} lays out one panel per child result
(cluster t-map with FWE-significant contours; bootstrap
dissimilarity histograms). To plot one panel at a time, call
\code{plot(result$results$cluster_test)} or
\code{plot(result$results$dissimilarity)} directly. Color
convention on the cluster panel matches the rest of the
package: blue = condition A larger; red = condition B larger;
black contours bound FWE-significant clusters.
}

\section{Testing the dissimilarity against chance}{

The bundled \code{\link[=rel_dissimilarity]{rel_dissimilarity()}} runs with its default
\code{null = "none"}, so the dissimilarity panel here reports the bootstrap
\emph{precision} interval only. That interval is biased upward by producer
resampling and almost always excludes zero even when the two
conditions do not differ, so it is not a test of "are these
conditions distinguishable?" (see \code{\link[=rel_dissimilarity]{rel_dissimilarity()}}). For an
above-chance test of overall magnitude, call
\code{rel_dissimilarity(signal_matrix_a, signal_matrix_b, null = "permutation")} directly and read \verb{$d_z} / \verb{$d_ratio} and a
permutation \emph{p} against the permutation null. The cluster test in
this report is already an FWER-controlled permutation test, so the
spatial panel does test against chance.
}

\section{Reliability metrics expect raw masks}{

Both downstream metrics are scale-sensitive: the cluster test
uses variance-based Welch t, and Euclidean distance in
\code{rel_dissimilarity()} is sensitive to any scaling. Inputs with
\code{attr(., "source") == "rendered"} error unless
\code{acknowledge_scaling = TRUE}.
}

\examples{
\dontrun{
# Two-condition pipeline with signal planted in different face regions
# (eyes vs mouth). The orchestrator runs the cluster test and the
# bootstrap dissimilarity in one call; significant pixels should
# localise around the eye and mouth regions.
sim_eyes  <- simulate_briefrc_data(
  n_per_condition = 20, n_trials = 60, conditions = "x",
  signal_region = "eyes", signal_strength = "strong", seed = 1
)
sim_mouth <- simulate_briefrc_data(
  n_per_condition = 20, n_trials = 60, conditions = "x",
  signal_region = "mouth", signal_strength = "strong", seed = 2
)
sig_eyes  <- ci_from_responses_briefrc(
  sim_eyes$data, noise_matrix = sim_eyes$noise_matrix)$signal_matrix
sig_mouth <- ci_from_responses_briefrc(
  sim_mouth$data, noise_matrix = sim_mouth$noise_matrix)$signal_matrix
res <- run_discriminability(sig_eyes, sig_mouth,
                             n_permutations = 500L, n_boot = 500L,
                             seed = 1)
print(res)
# Whole report (cluster panel + dissimilarity panel side by side):
plot(res)
# Or each panel on its own:
plot(res$results$cluster_test,
     main = "Eyes vs Mouth — cluster test")
plot(res$results$dissimilarity,
     main = "Eyes vs Mouth — dissimilarity")
}
}
\seealso{
\code{\link[=rel_cluster_test]{rel_cluster_test()}}, \code{\link[=rel_dissimilarity]{rel_dissimilarity()}},
\code{\link[=run_reliability]{run_reliability()}}, \code{\link[=run_discriminability_pairwise]{run_discriminability_pairwise()}}.
}
