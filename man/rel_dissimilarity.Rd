% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rel_dissimilarity.R
\name{rel_dissimilarity}
\alias{rel_dissimilarity}
\title{Between-condition dissimilarity with bootstrap confidence intervals}
\usage{
rel_dissimilarity(
  signal_matrix_a,
  signal_matrix_b,
  paired = FALSE,
  n_boot = 2000L,
  ci_level = 0.95,
  null = c("none", "permutation"),
  n_permutations = 2000L,
  mask = NULL,
  seed = NULL,
  progress = TRUE,
  acknowledge_scaling = FALSE
)
}
\arguments{
\item{signal_matrix_a, signal_matrix_b}{Pixels x participants,
base-subtracted. Row counts must match.}

\item{paired}{Logical. \code{FALSE} (default) for between-subjects:
participants resampled within A and B independently. \code{TRUE}
for within-subjects: A and B share a single resample index per
replicate so the paired covariance structure is preserved.}

\item{n_boot}{Bootstrap replicates. Default 2000.}

\item{ci_level}{Confidence level. Default 0.95.}

\item{null}{One of \code{"none"} (default) or \code{"permutation"}.
\code{"permutation"} builds an empirical chance baseline for the
Euclidean distance.}

\item{n_permutations}{Integer. Number of null iterations when
\code{null = "permutation"}. Default 2000.}

\item{mask}{Optional logical vector of length
\code{nrow(signal_matrix_a)} restricting the Euclidean / correlation
computation to a region.}

\item{seed}{Optional integer; RNG state restored on exit.}

\item{progress}{Show a \code{cli} progress bar.}

\item{acknowledge_scaling}{Logical. When \code{FALSE} (default), the
shared \code{assert_raw_signal()} helper errors on a known-rendered
matrix on either side.}
}
\value{
Object of class \code{rcisignal_rel_dissim}.
}
\description{
Quantifies overall dissimilarity between two conditions'
group-level classification images. The primary statistic is
Euclidean distance between the two group-mean CIs, reported
both raw and normalised by \code{sqrt(n_pixels)} for cross-resolution
comparability. Percentile bootstrap 95\% confidence intervals are
computed by resampling participants with replacement within each
condition.

Pair with \code{\link[=rel_cluster_test]{rel_cluster_test()}} when you also want to know
\strong{where} the two conditions differ.
}
\details{
For the observed statistics and each bootstrap replicate \code{i}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{mean_a      = rowMeans(signal_matrix_a)
mean_b      = rowMeans(signal_matrix_b)
observed_dist            = sqrt(sum((mean_a - mean_b)^2))
observed_dist_normalised = observed_dist / sqrt(n_pixels)
}\if{html}{\out{</div>}}

Percentile CI via base R \code{quantile()}; no \code{boot} dependency.
}
\section{Why Euclidean and not Pearson correlation as the primary}{

Two base-subtracted CIs share systematic image-domain spatial
structure (face shape, oval signal support, low-frequency
Gaussian-noise smoothness) that pushes their correlation above
zero even when the underlying mental representations are
unrelated. Absolute correlation values therefore do not cleanly
mean "these conditions are similar". Euclidean distance does
not share this failure mode. The Pearson correlation fields
(\verb{$correlation}, \verb{$boot_cor}, \verb{$ci_cor}, \verb{$boot_se_cor}) are
retained for backwards compatibility and slated for removal in
a future release.
}

\section{Reading the result}{

\itemize{
\item \verb{$euclidean}, observed Euclidean distance between group means
(primary statistic).
\item \verb{$euclidean_normalised}, \verb{$euclidean / sqrt(n_pixels)}. Use
for cross-resolution comparisons.
\item \verb{$boot_dist}, \verb{$ci_dist}, \verb{$boot_se_dist}, bootstrap
distribution, percentile CI, and SE of the Euclidean distance.
\item \verb{$null} (character), the null mode used.
\item \verb{$null_distribution}, when \code{null != "none"}: numeric vector
of per-iteration Euclidean distances under the chosen null.
\item \verb{$d_null_p95}, 95th percentile of the null distribution.
\item \verb{$d_z}, z-equivalent effect size:
\code{(observed_d - mean(null)) / sd(null)}.
\item \verb{$d_ratio}, observed Euclidean over the null median.
\item \verb{$correlation}, \verb{$boot_cor}, \verb{$ci_cor}, \verb{$boot_se_cor}:
Pearson correlation of the group means. Deprecated; see above.
\item \verb{$n_boot}, \verb{$ci_level}, \verb{$paired}, metadata.
}
}

\examples{
\dontrun{
# In a real pipeline, the two matrices come from earlier steps:
#   sig_a <- ci_from_responses_briefrc(responses_cond_a, ...)$signal_matrix
#   sig_b <- ci_from_responses_briefrc(responses_cond_b, ...)$signal_matrix
# For a self-contained demo we fabricate two small synthetic inputs:
n_pix  <- 32L * 32L
n_prod <- 20L
set.seed(1)
signal_matrix_a <- matrix(rnorm(n_pix * n_prod), n_pix, n_prod)
signal_matrix_b <- matrix(rnorm(n_pix * n_prod), n_pix, n_prod)

d <- rel_dissimilarity(signal_matrix_a, signal_matrix_b,
                       n_boot = 200L, seed = 1)
print(d)
}
}
\references{
Efron, B., & Tibshirani, R. J. (1994). \emph{An introduction to the
bootstrap}. Chapman & Hall / CRC.
}
\seealso{
\code{\link[=rel_cluster_test]{rel_cluster_test()}}, \code{\link[=run_discriminability]{run_discriminability()}}
}
