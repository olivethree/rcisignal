% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rel_dissimilarity.R
\name{rel_dissimilarity}
\alias{rel_dissimilarity}
\title{Between-condition dissimilarity with bootstrap confidence intervals}
\usage{
rel_dissimilarity(
  signal_matrix_a,
  signal_matrix_b,
  paired = FALSE,
  n_boot = 2000L,
  ci_level = 0.95,
  null = c("none", "permutation"),
  n_permutations = 2000L,
  mask = NULL,
  seed = NULL,
  progress = TRUE,
  acknowledge_scaling = FALSE
)
}
\arguments{
\item{signal_matrix_a, signal_matrix_b}{Pixels x participants,
base-subtracted. Row counts must match.}

\item{paired}{Logical. \code{FALSE} (default) for between-subjects:
participants resampled within A and B independently. \code{TRUE}
for within-subjects: A and B share a single resample index per
replicate so the paired covariance structure is preserved.}

\item{n_boot}{Bootstrap replicates. Default 2000.}

\item{ci_level}{Confidence level. Default 0.95.}

\item{null}{One of \code{"none"} (default) or \code{"permutation"}.
\code{"permutation"} builds an empirical chance baseline for the
Euclidean distance.}

\item{n_permutations}{Integer. Number of null iterations when
\code{null = "permutation"}. Default 2000.}

\item{mask}{Optional logical vector of length
\code{nrow(signal_matrix_a)} (column-major) restricting the
Euclidean / correlation computation to a region. Build with
\code{\link[=make_face_mask]{make_face_mask()}} (parametric oval and sub-regions) or
\code{\link[=read_face_mask]{read_face_mask()}} (PNG/JPEG mask).}

\item{seed}{Optional integer; RNG state restored on exit.}

\item{progress}{Show a \code{cli} progress bar.}

\item{acknowledge_scaling}{Logical. When \code{FALSE} (default), the
shared \code{assert_raw_signal()} helper errors on a known-rendered
matrix on either side.}
}
\value{
Object of class \code{rcisignal_rel_dissim}.
}
\description{
Quantifies overall dissimilarity between two conditions'
group-level classification images. The primary statistic is
Euclidean distance between the two group-mean CIs, reported
both raw and normalised by \code{sqrt(n_pixels)} for cross-resolution
comparability. Percentile bootstrap 95\% confidence intervals are
computed by resampling participants with replacement within each
condition.

Pair with \code{\link[=rel_cluster_test]{rel_cluster_test()}} when you also want to know
\strong{where} the two conditions differ.
}
\details{
For the observed statistics and each bootstrap replicate \code{i}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{mean_a      = rowMeans(signal_matrix_a)
mean_b      = rowMeans(signal_matrix_b)
observed_dist            = sqrt(sum((mean_a - mean_b)^2))
observed_dist_normalised = observed_dist / sqrt(n_pixels)
}\if{html}{\out{</div>}}

Percentile CI via base R \code{quantile()}; no \code{boot} dependency.
}
\section{Why Euclidean and not Pearson correlation as the primary}{

Two base-subtracted CIs share systematic image-domain spatial
structure (face shape, oval signal support, low-frequency
Gaussian-noise smoothness) that pushes their correlation above
zero even when the underlying mental representations are
unrelated. Absolute correlation values therefore do not cleanly
mean "these conditions are similar": two unrelated traits can
easily produce \code{r} well above zero from the shared image
scaffolding alone, so a "high" correlation does not by itself
license a similarity claim. Euclidean distance does not share
this baseline issue, which is why it is the primary statistic
here.

The Pearson correlation fields (\verb{$correlation}, \verb{$boot_cor},
\verb{$ci_cor}, \verb{$boot_se_cor}) are returned alongside it as a
secondary summary, for users who need to report \code{r} for
comparability with prior literature or with another analysis
pipeline. They are not recommended as a standalone similarity
score. If you do report \code{r}, interpret it \strong{relatively} (the
ordering of \code{r} across multiple condition pairs is more
defensible than any single absolute value) and \strong{benchmark
against a permutation null} rather than against zero. The
image-domain scaffolding shifts the chance baseline upward;
"above zero" is not the right comparison.
}

\section{Reading the plot}{

\code{plot()} on the returned object renders the bootstrap
distributions as two side-by-side histograms (Euclidean
distance on the left, Pearson r on the right). The Pearson
panel is rendered in grey because it is a secondary summary,
not recommended as a standalone similarity score; see the
description for why and how to use it carefully if needed.
\itemize{
\item The shaded vertical band marks the percentile CI at
\code{ci_level}.
\item The vertical line marks the \emph{observed} statistic on the
real data (not a bootstrap mean).
\item A non-overlapping CI band away from zero on the Euclidean
panel indicates the two group-mean CIs sit a non-trivial
distance apart in pixel space, robust to participant-level
resampling. The numbers are returned in \verb{$ci_dist}.
\item For visual comparison across multiple contrasts, pass each
\code{rel_dissimilarity()} result to \code{\link[=plot_dissimilarity_grid]{plot_dissimilarity_grid()}},
which lays them out as labelled CI bars on a shared axis.
\item For a \emph{spatial} picture of where the two conditions differ,
pair this with \code{\link[=rel_cluster_test]{rel_cluster_test()}} (or use
\code{\link[=run_discriminability]{run_discriminability()}} to run both in one call).
}
}

\section{Reading the result}{

\itemize{
\item \verb{$euclidean}, observed Euclidean distance between group means
(primary statistic).
\item \verb{$euclidean_normalised}, \verb{$euclidean / sqrt(n_pixels)}. Use
for cross-resolution comparisons.
\item \verb{$boot_dist}, \verb{$ci_dist}, \verb{$boot_se_dist}, bootstrap
distribution, percentile CI, and SE of the Euclidean distance.
\item \verb{$null} (character), the null mode used.
\item \verb{$null_distribution}, when \code{null != "none"}: numeric vector
of per-iteration Euclidean distances under the chosen null.
\item \verb{$d_null_p95}, 95th percentile of the null distribution.
\item \verb{$d_z}, z-equivalent effect size:
\code{(observed_d - mean(null)) / sd(null)}.
\item \verb{$d_ratio}, observed Euclidean over the null median.
\item \verb{$correlation}, \verb{$boot_cor}, \verb{$ci_cor}, \verb{$boot_se_cor}:
Pearson correlation of the group means and its bootstrap
summaries. Secondary; not recommended as a standalone
similarity score (see the "Why Euclidean" section). If
reporting, prefer relative comparisons across pairs against
a permutation null.
\item \verb{$n_boot}, \verb{$ci_level}, \verb{$paired}, metadata.
}
}

\examples{
\dontrun{
# Minimal call-signature demo with two synthetic inputs.
n_pix  <- 32L * 32L
n_prod <- 20L
set.seed(1)
signal_matrix_a <- matrix(rnorm(n_pix * n_prod), n_pix, n_prod)
signal_matrix_b <- matrix(rnorm(n_pix * n_prod), n_pix, n_prod)
rel_dissimilarity(signal_matrix_a, signal_matrix_b,
                  n_boot = 200L, seed = 1)
}

\dontrun{
# Same function, richer input: signal planted in different face regions
# (eyes vs mouth). The Euclidean distance and its bootstrap CI should
# be well above zero, reflecting genuine spatial divergence.
sim_eyes  <- simulate_briefrc_data(
  n_per_condition = 20, n_trials = 60, conditions = "x",
  signal_region = "eyes", signal_strength = "strong", seed = 1
)
sim_mouth <- simulate_briefrc_data(
  n_per_condition = 20, n_trials = 60, conditions = "x",
  signal_region = "mouth", signal_strength = "strong", seed = 2
)
sig_eyes  <- ci_from_responses_briefrc(
  sim_eyes$data, noise_matrix = sim_eyes$noise_matrix)$signal_matrix
sig_mouth <- ci_from_responses_briefrc(
  sim_mouth$data, noise_matrix = sim_mouth$noise_matrix)$signal_matrix
d <- rel_dissimilarity(sig_eyes, sig_mouth, n_boot = 500L, seed = 1)
# Bootstrap distribution + observed Euclidean + 95\% CI band.
plot(d, main = "Eyes vs Mouth: bootstrap dissimilarity")
}
}
\references{
Efron, B., & Tibshirani, R. J. (1994). \emph{An introduction to the
bootstrap}. Chapman & Hall / CRC.
}
\seealso{
\code{\link[=rel_cluster_test]{rel_cluster_test()}}, \code{\link[=run_discriminability]{run_discriminability()}}
}
