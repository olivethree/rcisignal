% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shift_mask.R
\name{shift_mask}
\alias{shift_mask}
\title{Shift a face-region mask by a number of pixels}
\usage{
shift_mask(mask, vertical = 0L, horizontal = 0L, img_dims = NULL)
}
\arguments{
\item{mask}{Logical vector of length \code{prod(img_dims)}
(column-major, as returned by \code{\link[=make_face_mask]{make_face_mask()}}) or a
logical matrix.}

\item{vertical, horizontal}{Integer pixel offsets. Positive
\code{vertical} moves the mask up (toward the top of the image),
negative \code{vertical} moves it down. Positive \code{horizontal}
moves the mask right, negative moves it left. Defaults are
\code{0} (no shift).}

\item{img_dims}{Integer \code{c(nrow, ncol)} (or a single integer
for a square image). Required when \code{mask} is a vector;
ignored when \code{mask} is already a matrix.}
}
\value{
Logical mask of the same shape as the input
(matrix-in, matrix-out; vector-in, vector-out).
}
\description{
Slides a logical mask along the two image axes and returns the
shifted mask in the same shape as the input. Convenience
wrapper around the manual recipe shown in vignette section 4.5
for tuning an \emph{elliptical} \code{make_face_mask()} region (\code{"full"},
\code{"nose"}, \code{"mouth"}, \code{"upper_face"}, \code{"lower_face"}) on a base
image whose features sit a few pixels off the default geometry.

For the \emph{rectangle} regions (\code{"eyes"}, \code{"left_eye"},
\code{"right_eye"}), prefer the \code{region_bounds} argument of
\code{\link[=make_face_mask]{make_face_mask()}}: passing
\code{region_bounds = c(row_min, row_max, col_min, col_max)} in 0-1
image fractions is more precise than counting pixels and is
the canonical tuning route for those regions.

Pixels shifted off the image are dropped, and the newly exposed
edge is filled with \code{FALSE} (i.e., outside the mask).
}
\examples{
# Reshape the default mouth mask, slide it up 20 pixels on a
# 256-pixel image (about 8\% of the height), and confirm it
# still has the right number of TRUE pixels.
m <- make_face_mask(c(256L, 256L), region = "mouth")
shifted <- shift_mask(m, vertical = 20L, img_dims = c(256L, 256L))
identical(sum(m), sum(shifted))

# Matrix-in, matrix-out: shift up 20 and right 8.
mat <- matrix(m, 256L, 256L)
shifted_mat <- shift_mask(mat, vertical = 20L, horizontal = 8L)
dim(shifted_mat)
}
\seealso{
\code{\link[=make_face_mask]{make_face_mask()}} (the \code{region_bounds} route for the
rectangle regions), \code{\link[=plot_mask_overlay]{plot_mask_overlay()}}, \code{\link[=plot_face_mask]{plot_face_mask()}}.
}
