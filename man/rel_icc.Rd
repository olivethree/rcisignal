% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rel_icc.R
\name{rel_icc}
\alias{rel_icc}
\title{Intraclass correlation coefficients via direct mean squares}
\usage{
rel_icc(
  signal_matrix,
  variants = c("3_1", "3_k"),
  mask = NULL,
  acknowledge_scaling = FALSE
)
}
\arguments{
\item{signal_matrix}{Pixels x participants (targets x raters),
base-subtracted.}

\item{variants}{Character vector of which ICC variants to return.
Subset of \code{c("3_1", "3_k", "2_1", "2_k")}. Defaults to
\code{c("3_1", "3_k")}.}

\item{mask}{Optional logical vector of length
\code{nrow(signal_matrix)} restricting computation to a region
(e.g., from \code{\link[=make_face_mask]{make_face_mask()}} or \code{\link[=read_face_mask]{read_face_mask()}}).}

\item{acknowledge_scaling}{Logical. When \code{FALSE} (default), the
shared \code{assert_raw_signal()} helper errors on a known-rendered
matrix.}
}
\value{
Object of class \code{rcisignal_rel_icc}.
}
\description{
Reports the reliability of producer-level CI signals as a single
scalar (or two scalars) per condition. Use \strong{ICC(3,1)} to ask
"how informative is one producer's CI as a noisy estimate of the
group pattern?" and \strong{ICC(3,k)} to ask "how stable is the
group-mean CI this experiment produced?". Most papers report
ICC(3,k) as the headline.
}
\details{
Reports \strong{ICC(3,1)} and \strong{ICC(3,k)}, two-way mixed model with
pixels fixed and participants random. Pixels are a fixed
\verb{img_size x img_size} grid (not a random sample), so ICC(2,*) is
mis-specified even when numerically similar. ICC(2,1) and
ICC(2,k) are available via \code{variants} for users whose reviewers
explicitly ask.

Computed directly from ANOVA mean squares (never via
\code{psych::ICC()}, which allocates intermediates that blow memory
on a 262,144 x 30 matrix).
}
\section{What this ICC is, and is not}{

Most ICCs reported in the reverse-correlation literature are
trait-rating reliability (phase-2 raters scoring CIs on trait
dimensions). rcisignal's ICC is structurally different: it
operates on the pixel-level signal produced by the original
producers. No phase-2 rating study is involved. This sidesteps
the two-phase design Cone, Brown-Iannuzzi, Lei, & Dotsch (2021)
showed inflates Type I error.
}

\section{Reading the result}{

\itemize{
\item \verb{$icc_3_1}, \verb{$icc_3_k}, two-way-mixed ICCs for single rater
and average rater respectively. \verb{$icc_2_1} / \verb{$icc_2_k} only
present if requested via \code{variants}.
\item \verb{$ms_rows}, \verb{$ms_cols}, \verb{$ms_error}, the underlying ANOVA mean
squares.
\item \verb{$n_raters} (= participants), \verb{$n_targets} (= pixels),
\verb{$model}, \verb{$variants}.
}
}

\section{Common mistakes}{

\itemize{
\item Asking for ICC(2,\emph{) because a reviewer expects it. ICC(2,})
treats pixels as a random sample from a pixel population, which
they aren't. Numbers are usually close to ICC(3,*) at high
pixel counts but the model is mis-specified.
\item Comparing this ICC to a phase-2 trait-rating ICC from a
different paper. Different statistical objects.
}
}

\section{Reliability metrics expect raw masks}{

ICC is variance-based and strongly sensitive to any scaling
step. Inputs with \code{attr(., "source") == "rendered"} (set
automatically by Mode 1 readers like \code{\link[=extract_signal]{extract_signal()}}) error
unless \code{acknowledge_scaling = TRUE}.
}

\examples{
\dontrun{
# In a real pipeline, signal_matrix comes from earlier steps:
#   signal_matrix <- ci_from_responses_briefrc(...)$signal_matrix
# For a self-contained demo we fabricate a small synthetic input:
n_pix  <- 32L * 32L
n_prod <- 20L
set.seed(1)
signal_matrix <- matrix(rnorm(n_pix * n_prod), n_pix, n_prod)

r <- rel_icc(signal_matrix)
print(r)
}
}
\references{
Shrout, P. E., & Fleiss, J. L. (1979). Intraclass correlations:
uses in assessing rater reliability. \emph{Psychological Bulletin},
86(2), 420-428. \doi{10.1037/0033-2909.86.2.420}

McGraw, K. O., & Wong, S. P. (1996). Forming inferences about some
intraclass correlation coefficients. \emph{Psychological Methods},
1(1), 30-46. \doi{10.1037/1082-989X.1.1.30}

Cone, J., Brown-Iannuzzi, J. L., Lei, R., & Dotsch, R. (2021).
Type I error is inflated in the two-phase reverse correlation
procedure. \emph{Social Psychological and Personality Science},
12(5), 760-768. \doi{10.1177/1948550620938616}
}
\seealso{
\code{\link[=rel_split_half]{rel_split_half()}}, \code{\link[=rel_loo]{rel_loo()}}, \code{\link[=run_reliability]{run_reliability()}}
}
