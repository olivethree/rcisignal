% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ci_from_responses_2ifc.R
\name{ci_from_responses_2ifc}
\alias{ci_from_responses_2ifc}
\title{Compute individual 2IFC CIs from trial-level responses}
\usage{
ci_from_responses_2ifc(
  responses,
  rdata_path = NULL,
  stimuli = NULL,
  base_image = NULL,
  participant_col = "participant_id",
  stimulus_col = "stimulus",
  response_col = "response",
  scaling = "autoscale",
  keep_rendered = FALSE,
  targetpath = tempfile("rcisignal_2ifc_"),
  save_as_png = FALSE
)
}
\arguments{
\item{responses}{A data frame with one row per trial. Must
contain \code{participant_col} (producer id), the column named by
\code{stimulus_col} (stimulus id, integer, index into the rcicr
noise pool), and \code{response_col} with values in \verb{\{-1, +1\}}.}

\item{rdata_path}{Path to the \code{.Rdata} file produced by
\code{\link[rcicr:generateStimuli2IFC]{rcicr::generateStimuli2IFC()}}. Either \code{rdata_path} or
\code{stimuli} must be supplied; if both are given \code{stimuli} wins.}

\item{stimuli}{In-memory stimuli list as returned in
\verb{$stimuli} by \code{\link[=simulate_2ifc_data]{simulate_2ifc_data()}}. Use this in place of
\code{rdata_path} when the simulation has been saved with
\code{\link[=saveRDS]{saveRDS()}} and reloaded in a different R session: the path
stored on \verb{$rdata_path} no longer resolves, but \verb{$stimuli}
is self-contained. Internally the list is written to a
fresh tempdir-backed \code{.Rdata} before the call into rcicr.}

\item{base_image}{Base face image. Three forms are accepted,
matching \code{\link[=ci_from_responses_briefrc]{ci_from_responses_briefrc()}}:
\itemize{
\item a numeric matrix in \verb{[0, 1]} (used directly),
\item a single string path to a PNG / JPEG, or
\item a single string label naming an entry in the rdata's
\code{base_faces} list (the historical 2IFC form).
When \code{NULL}, the rdata's single base is used; if the rdata
contains more than one base, the call aborts and lists the
available labels. Matrix and path forms are injected into a
temporary copy of the rdata under a synthetic label so the
rcicr call sees the same structure it always has.
}}

\item{participant_col, stimulus_col, response_col}{Column names in
\code{responses}.}

\item{scaling}{rcicr scaling option; one of \code{"autoscale"},
\code{"independent"}, \code{"constant"}, \code{"none"}. Passed through to
\code{rcicr::batchGenerateCI2IFC()}. Scaling only affects the
\verb{$combined} (rendered) image, not \verb{$ci}, so the returned
\verb{$signal_matrix} is the raw mask regardless of this argument.}

\item{keep_rendered}{If \code{TRUE}, also extract rcicr's \verb{$combined}
image (base + scaled noise) per producer and stack into
\verb{$rendered_ci}. Default \code{FALSE}. Visualization only.}

\item{targetpath}{Where rcicr writes PNGs. Defaults to an
auto-cleaned tempdir so the working directory is not polluted.}

\item{save_as_png}{Whether rcicr writes individual CI PNGs.
Defaults to \code{FALSE} for speed in a pure reliability pipeline.}
}
\value{
A list with \code{signal_matrix}, optionally \code{rendered_ci},
\code{participants}, \code{img_dims}, \code{scaling}, and \code{rcicr_result}.
}
\description{
Thin wrapper around \code{\link[rcicr:batchGenerateCI2IFC]{rcicr::batchGenerateCI2IFC()}} that handles
the known rcicr-integration gotchas and exposes a uniform return
shape across the 2IFC and Brief-RC paths.

Use this when you have 2IFC trial-level responses and want the
package to compute individual CIs ready for the reliability
metrics.
}
\details{
What the wrapper does for you:
\itemize{
\item Attaches \code{foreach}, \code{tibble}, \code{dplyr} at runtime
(\verb{\%dopar\%} / \code{tribble} / \verb{\%>\%} are not namespace-prefixed
inside rcicr).
\item Matches the \code{.Rdata} extension case-insensitively (rcicr
writes lowercase on some filesystems).
\item Extracts the per-participant CI noise component (\verb{$ci}) from
the rcicr result list and stacks it into a pixels x
participants signal matrix, already base-subtracted, ready
for \verb{rel_*()}.
\item When \code{keep_rendered = TRUE}, also extracts the rendered
\verb{$combined} image and stacks it as \verb{$rendered_ci} for
visualization (display only, not for downstream stats).
}

\code{rcicr} must be installed (it is a Suggests dep; install with
\code{remotes::install_github("rdotsch/rcicr")} if needed).
}
\section{Reading the result}{

\itemize{
\item \verb{$signal_matrix} is the \strong{raw mask} (rcicr's \verb{$ci} per
producer). This is what every \verb{rel_*} function expects.
\item \verb{$rendered_ci}, present when \code{keep_rendered = TRUE}, is the
\code{base + scaling(mask)} image rcicr would have written to PNG.
\strong{Visualization only.}
\item \verb{$rcicr_result} is the raw return value of
\code{\link[rcicr:batchGenerateCI2IFC]{rcicr::batchGenerateCI2IFC()}}.
}
}

\section{Common mistakes}{

\itemize{
\item Passing \verb{$rendered_ci} to \verb{rel_*} functions; it carries the
scaling step, which distorts variance-based metrics.
\item Forgetting that the warning about Mode 1 also applies if you
later read \verb{$rendered_ci} back from PNG instead of using
\verb{$signal_matrix} directly.
}
}

\examples{
\dontrun{
# In your study, you replace the simulated inputs with your own:
#   - `sim$data` -> your responses data frame (CSV via read_responses())
#   - `sim$rdata_path` -> path to the rcicr stimuli .Rdata file used to
#     generate your stimuli.
# The `simulate_2ifc_data()` helper just produces the same shapes so the
# example below is runnable end-to-end.
sim <- simulate_2ifc_data(n_per_condition = 10, n_trials = 60, seed = 1)
res <- ci_from_responses_2ifc(sim$data, rdata_path = sim$rdata_path)
dim(res$signal_matrix)   # n_pixels x n_producers
rel_split_half(res$signal_matrix, n_permutations = 200L, seed = 1)

# Same call, but using the portable in-memory stimuli list -- the
# form that survives saveRDS()/readRDS() across R sessions.
res2 <- ci_from_responses_2ifc(sim$data, stimuli = sim$stimuli)

# Supply a base image as a path or matrix (same shapes as Brief-RC).
res3 <- ci_from_responses_2ifc(sim$data, rdata_path = sim$rdata_path,
                               base_image = sim$base_image_path)
res4 <- ci_from_responses_2ifc(sim$data, rdata_path = sim$rdata_path,
                               base_image = sim$base_face)
}
}
\seealso{
\code{\link[=ci_from_responses_briefrc]{ci_from_responses_briefrc()}},
\code{\link[rcicr:batchGenerateCI2IFC]{rcicr::batchGenerateCI2IFC()}},
\code{\link[=group_ci]{group_ci()}} (stage 2, optional)
}
