% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_infoval_summary.R
\name{compute_infoval_summary}
\alias{compute_infoval_summary}
\title{Per-participant information-value (infoVal) summary}
\usage{
compute_infoval_summary(
  responses,
  method = c("2ifc", "briefrc"),
  rdata,
  baseimage = "base",
  col_participant = "participant_id",
  col_stimulus = "stimulus",
  col_response = "response",
  iter = 10000L,
  threshold = 1.96,
  ...
)
}
\arguments{
\item{responses}{Data frame with one row per trial. Required
columns: \code{participant_id}, \code{stimulus}, \code{response} (values in
\verb{\{-1, +1\}}). Load yours from CSV via \code{\link[=read_responses]{read_responses()}} or
\code{\link[utils:read.table]{utils::read.csv()}}; column names are configurable via the
\verb{col_*} arguments.}

\item{method}{\code{"2ifc"} (supported) or \code{"briefrc"} (returns \code{"skip"}).}

\item{rdata}{Path to the rcicr \code{.RData} file that produced the
stimuli. Required for 2IFC.}

\item{baseimage}{Name of the base image used at generation time
(the key in \code{base_face_files} in the rdata). Default \code{"base"}.}

\item{col_participant, col_stimulus, col_response}{Column names.}

\item{iter}{Number of iterations for rcicr's reference-distribution
simulation. Default \code{10000} (the rcicr-recommended value).}

\item{threshold}{Numeric. Participants with infoVal below this are
flagged as likely noise. Default \code{1.96}.}

\item{...}{Unused.}
}
\value{
An \code{\link[=rcisignal_diag_result]{rcisignal_diag_result()}} object. For 2IFC, \code{data$per_participant}
has one row per participant with \code{participant_id}, \code{infoval}, and
\code{above_threshold} (logical: \code{infoval >= threshold}). For Brief-RC,
a \code{"skip"} result pointing the user at \code{\link[=diagnose_infoval]{diagnose_infoval()}}.
}
\description{
Computes per-participant information value (infoVal; Brinkman et al.,
2019) via the original \code{rcicr} 2IFC pipeline and returns a tidy
per-participant table. This is the thin compatibility wrapper around
\code{\link[rcicr:computeInfoVal2IFC]{rcicr::computeInfoVal2IFC()}}. For a richer, paradigm-agnostic
diagnostic (group-mean z, random-responder calibration check,
face-mask z-lift, interpretation bullets), see \code{\link[=diagnose_infoval]{diagnose_infoval()}}.
}
\details{
Reported per-producer infoVal is paradigm- and target-dependent.
Brinkman et al. (2019, p. 12) report 68\% (lab) and 54\% (online) of
2IFC gender participants clearing z = 1.96, with mean per-participant
infoVal 3.9 (lab) and 2.9 (online) and most participants in the 3-4
range. Schmitz et al. (2024) report Brief-RC infoVals below 1.96
across all conditions in both of their experiments. The status
returned here therefore does not
\code{"fail"} on a per-participant headcount alone: it returns \code{"pass"}
when the median per-participant z is positive and \code{"warn"}
otherwise. Use \code{\link[=diagnose_infoval]{diagnose_infoval()}} when you need a proper verdict
(random-responder calibration + group-mean z + masking).

The 2IFC path delegates to \code{\link[rcicr:batchGenerateCI2IFC]{rcicr::batchGenerateCI2IFC()}} and
\code{\link[rcicr:computeInfoVal2IFC]{rcicr::computeInfoVal2IFC()}} from the original \code{rcicr} package
(Dotsch, 2016; v1.0.1). The original \code{rcicr} uses a pool-size
reference distribution and does not expose a Brief-RC path. The Brief-RC route
here returns a \code{"skip"} result and points users at
\code{\link[=diagnose_infoval]{diagnose_infoval()}}, which uses an in-package native Brief-RC
implementation with a per-trial-count reference distribution.

\strong{Side effect (2IFC).} \code{rcicr} caches a reference distribution inside
the supplied \code{rdata} file on the first call. Subsequent calls reuse
it. Copy your \code{rdata} beforehand if you want the original untouched.
}
\examples{
\dontrun{
sim <- simulate_2ifc_data(n_per_condition = 10, n_trials = 60, seed = 1)
compute_infoval_summary(sim$data, method = "2ifc",
                        rdata = sim$rdata_path, iter = 200L)
}

}
\references{
Brinkman, L., Goffin, S., van de Schoot, R., van Haren, N. E., Dotsch,
R., & Aarts, H. (2019). Quantifying the informational value of
classification images. \emph{Behavior Research Methods}, 51(5), 2059--2073.

Dotsch, R. (2016). \emph{rcicr: Reverse-correlation image-classification
toolbox} [R package].
\url{https://github.com/rdotsch/rcicr}
}
