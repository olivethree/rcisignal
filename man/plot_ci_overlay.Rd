% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ci_overlay.R
\name{plot_ci_overlay}
\alias{plot_ci_overlay}
\title{Render a group CI as a translucent agreement-map overlay on a base image}
\usage{
plot_ci_overlay(
  signal_matrix,
  base_image,
  img_dims = NULL,
  test = NULL,
  mask = NULL,
  threshold = NULL,
  alpha_max = 0.7,
  palette = c("diverging"),
  contour_col = "black",
  contour_lwd = 1,
  main = NULL
)
}
\arguments{
\item{signal_matrix}{Pixels x participants; the producer mean is
computed and rendered as the heatmap layer. Alternatively, a
numeric vector of length \code{prod(img_dims)} (e.g.
\code{agreement_map_test(...)$observed_t}) which is rendered
directly.}

\item{base_image}{Either a numeric matrix (\verb{nrow x ncol},
grayscale, values in 0-1) or a path to a PNG/JPEG file. When
it's a path, the image is read as grayscale (BT.709 luminance
for RGB inputs) the same way as \code{\link[=read_cis]{read_cis()}}.}

\item{img_dims}{Integer \code{c(nrow, ncol)}. If \code{NULL}, inferred
from \code{attr(signal_matrix, "img_dims")} or, when \code{signal_matrix}
is a vector, the dimensions of \code{base_image}.}

\item{test}{Optional \code{\link[=agreement_map_test]{agreement_map_test()}} result. When
supplied, significance contours (around \code{significant_mask})
are drawn on top of the heatmap.}

\item{mask}{Optional logical vector of length
\code{prod(img_dims)} restricting the visible overlay region.
Pixels outside the mask render as the bare base image.
Build with \code{\link[=make_face_mask]{make_face_mask()}} (parametric oval and
sub-regions) or \code{\link[=read_face_mask]{read_face_mask()}} (PNG/JPEG mask file). The
mask is column-major to match the package's image
vectorisation convention.}

\item{threshold}{Optional numeric. Pixels with \verb{|signal| < threshold} are rendered as fully transparent (only the base
shows through). Default \code{NULL} (no threshold).}

\item{alpha_max}{Numeric in \verb{[0, 1]}. Maximum opacity at the
peak of the signal magnitude. Default 0.7.}

\item{palette}{Character. Currently only \code{"diverging"} is
implemented (red-white-blue). Future-proofed by argument.}

\item{contour_col, contour_lwd}{Significance-contour colour and
line width when \code{test} is supplied. Defaults: \code{"black"}, \code{1.0}.}

\item{main}{Optional plot title.}
}
\value{
Invisibly the composed \verb{nrow x ncol x 3} raster. The
plot is drawn on the active device as a side effect.
}
\description{
Plots the producer-mean classification image (or the per-pixel
\emph{t}-map from \code{\link[=agreement_map_test]{agreement_map_test()}}) as a diverging-palette
translucent layer composited over the base face image. Useful as
a publication-grade single figure that combines anatomical
context (the base) with the empirical signal (the CI / agreement
map). Optionally overlays significance contours from an
\code{\link[=agreement_map_test]{agreement_map_test()}} result.

Pure base graphics — no \code{grid} / \code{ggplot2} dependency. Returns
invisibly the composed \verb{nrow x ncol x 3} numeric raster so it
can be saved to disk via \code{\link[png:writePNG]{png::writePNG()}} / \code{\link[jpeg:writeJPEG]{jpeg::writeJPEG()}}
if needed.
}
\section{Reading the plot}{

\itemize{
\item \strong{Colour} encodes the sign of the producer-mean signal at each
pixel. Blue = positive (producers' average mask is brighter than
the base at that pixel); red = negative (darker than the base);
pixels rendered as the bare base = at or near zero.
\item \strong{Opacity} encodes the magnitude of the signal, scaled to
\code{alpha_max} at the global peak \verb{|signal|}. Faint colour means
weak agreement; saturated colour means a strong, consistent
producer-mean deflection.
\item \strong{Black contours} (only drawn when \code{test} is supplied) trace
the boundary of the FWE-significant pixel set returned by
\code{\link[=agreement_map_test]{agreement_map_test()}} at its \code{alpha}. Pixels inside the
contour are individually significant under the max-|t| null;
pixels outside are not.
\item Colour convention matches \code{\link[=plot_agreement_map]{plot_agreement_map()}} and the
cluster-test plots so the same group CI reads consistently
across the package.
}
}

\examples{
\dontrun{
# Minimal call-signature demo with synthetic inputs and a flat base.
n_side <- 32L
n_pix  <- n_side * n_side
n_prod <- 20L
set.seed(1)
signal_matrix <- matrix(rnorm(n_pix * n_prod), n_pix, n_prod)
base_image    <- matrix(0.5, n_side, n_side)
plot_ci_overlay(signal_matrix, base_image,
                img_dims = c(n_side, n_side))
}

\dontrun{
# Same function, richer input: plant a signal in the viewer's
# left-eye rectangle and overlay it on the simulator's base
# face. That sub-region should light up against the anatomical
# context, with the right-eye rectangle and the rest of the
# face left untouched. (Swap signal_region for "right_eye",
# "eyes", "mouth", etc. to demo any other region.)
sim <- simulate_briefrc_data(
  n_per_condition = 20, n_trials = 60, conditions = "target",
  signal_region = "left_eye", signal_strength = "strong",
  seed = 1
)
cis  <- ci_from_responses_briefrc(sim$data, noise_matrix = sim$noise_matrix)
plot_ci_overlay(cis$signal_matrix, sim$base_face)
}

\dontrun{
# Same overlay with FWE-significant pixels outlined: feed an
# agreement_map_test() result via `test = `. Black contours trace
# the boundary of pixels significant at the test's alpha.
sim   <- simulate_briefrc_data(
  n_per_condition = 20, n_trials = 60, conditions = "target",
  signal_region = "eyes", signal_strength = "strong", seed = 1
)
cis   <- ci_from_responses_briefrc(sim$data, noise_matrix = sim$noise_matrix)
agree <- agreement_map_test(cis$signal_matrix,
                            n_permutations = 500L, seed = 1)
plot_ci_overlay(cis$signal_matrix, sim$base_face,
                test = agree,
                main = "CI overlay + significant pixels")
}
}
\seealso{
\code{\link[=plot_agreement_map]{plot_agreement_map()}} for the producer-agreement
counterpart (signed t or \verb{|t|}, optionally over the same base);
\code{\link[=agreement_map_test]{agreement_map_test()}} for FWE-controlled significance contours;
\code{\link[=make_face_mask]{make_face_mask()}} / \code{\link[=read_face_mask]{read_face_mask()}} for the optional \code{mask}.
}
