% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_stimulus_alignment.R
\name{check_stimulus_alignment}
\alias{check_stimulus_alignment}
\title{Check stimulus ids align with the stimulus pool}
\usage{
check_stimulus_alignment(
  responses,
  method = c("2ifc", "briefrc"),
  rdata = NULL,
  noise_matrix = NULL,
  col_stimulus = "stimulus",
  ...
)
}
\arguments{
\item{responses}{Data frame with one row per trial. Required
columns: \code{participant_id}, \code{stimulus}, \code{response} (values in
\verb{\{-1, +1\}}). Load yours from CSV via \code{\link[=read_responses]{read_responses()}} or
\code{\link[utils:read.table]{utils::read.csv()}}; column names are configurable via the
\verb{col_*} arguments.}

\item{method}{Either \code{"2ifc"} or \code{"briefrc"}.}

\item{rdata}{Path to the \code{.RData} file produced by
\code{rcicr::generateStimuli2IFC()}. Required when \code{method = "2ifc"}.}

\item{noise_matrix}{Path to the Brief-RC noise-matrix text file, or a
numeric matrix already in memory. Required when \code{method = "briefrc"}.}

\item{col_stimulus}{Name of the stimulus column in \code{responses}.}

\item{...}{Unused.}
}
\value{
An \code{\link[=rcisignal_diag_result]{rcisignal_diag_result()}} object. \code{data} contains
\code{pool_size} (number of stimuli in the pool), \code{n_stim_used} (unique
stimuli referenced in the data), \code{out_of_range} (stimulus ids
outside \verb{1..pool_size}), and \code{unused_stimuli} (pool ids never
referenced).
}
\description{
Verifies that the \code{stimulus} column in the response data refers to
valid indices in the stimulus pool defined by the supplied \code{.RData}
(2IFC) or noise-matrix file (Brief-RC). An off-by-one error in the
response export, or a mismatch between the pool used in the experiment
and the pool passed to the diagnostics, is the most common silent
source of wrong CIs — this check catches both.
}
\examples{
mat <- matrix(rnorm(16384 * 10, sd = 0.05), nrow = 16384, ncol = 10)
responses <- data.frame(
  participant_id = rep(1:2, each = 20),
  stimulus       = sample.int(10, 40, replace = TRUE),
  response       = sample(c(-1, 1), 40, replace = TRUE)
)
check_stimulus_alignment(
  responses, method = "briefrc", noise_matrix = mat
)

}
