% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_response_coding.R
\name{check_response_coding}
\alias{check_response_coding}
\title{Check that response values use the expected coding}
\usage{
check_response_coding(
  responses,
  method = c("2ifc", "briefrc"),
  col_response = "response",
  ...
)
}
\arguments{
\item{responses}{Data frame with one row per trial. Required
columns: \code{participant_id}, \code{stimulus}, \code{response} (values in
\verb{\{-1, +1\}}). Load yours from CSV via \code{\link[=read_responses]{read_responses()}} or
\code{\link[utils:read.table]{utils::read.csv()}}; column names are configurable via the
\verb{col_*} arguments.}

\item{method}{Either \code{"2ifc"} or \code{"briefrc"}.}

\item{col_response}{Name of the column holding response values.
Defaults to \code{"response"}.}

\item{...}{Unused. Reserved for consistency with other check functions.}
}
\value{
An \code{\link[=rcisignal_diag_result]{rcisignal_diag_result()}} object.
}
\description{
For the 2IFC pipeline, responses must be coded as \verb{\{-1, 1\}}. Common
miscodings (\verb{\{0, 1\}} or \verb{\{1, 2\}}) produce classification images that
look plausible but are shifted or inverted. This check detects the
common miscodings explicitly and returns a status with a fix suggestion.
}
\details{
For the Brief-RC pipeline, responses may be binary \verb{\{-1, 1\}} or
continuous weights. Anything numeric and finite is accepted.
}
\examples{
responses <- data.frame(
  participant_id = rep(1, 10),
  stimulus       = 1:10,
  response       = sample(c(-1, 1), 10, replace = TRUE)
)
check_response_coding(responses, method = "2ifc")

}
