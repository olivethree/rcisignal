% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rel_split_half.R
\name{rel_split_half}
\alias{rel_split_half}
\title{Permuted split-half reliability with Spearman-Brown correction}
\usage{
rel_split_half(
  signal_matrix,
  n_permutations = 2000L,
  null = c("none", "permutation", "random_responders"),
  noise_matrix = NULL,
  mask = NULL,
  seed = NULL,
  progress = TRUE
)
}
\arguments{
\item{signal_matrix}{Pixels x participants, base-subtracted.}

\item{n_permutations}{Integer number of random splits. Default
2000.}

\item{null}{One of \code{"none"} (default), \code{"permutation"}, or
\code{"random_responders"}.
\itemize{
\item \code{"none"} skips the empirical-null computation;
\verb{$null_distribution} is \code{NULL}.
\item \code{"permutation"}: at each iteration, generate fresh Gaussian
noise per producer (no shared spatial structure); recompute
\code{r_hh} per iteration. The empirical distribution becomes the
chance baseline.
\item \code{"random_responders"}: simulate \code{ncol(signal_matrix)}
producers responding at chance using \code{genMask()} machinery
on the supplied \code{noise_matrix}. Closer to the empirical
chance baseline of an actual RC experiment.
}}

\item{noise_matrix}{Pixels x pool-size numeric matrix of noise
patterns. Required when \code{null = "random_responders"}.}

\item{mask}{Optional logical vector of length
\code{nrow(signal_matrix)} (column-major) restricting computation
to a region of the image. Build with \code{\link[=make_face_mask]{make_face_mask()}}
(parametric oval and sub-regions) or \code{\link[=read_face_mask]{read_face_mask()}}
(PNG/JPEG mask).}

\item{seed}{Optional integer; if set, results are reproducible.
The caller's global RNG state is restored on exit.}

\item{progress}{Show a \code{cli} progress bar.}
}
\value{
An object of class \code{rcisignal_rel_split_half}. Fields
described in \strong{Reading the result} above.
}
\description{
Measures how stable a condition's group-level CI is across random
halves of its producer set. For each of \code{n_permutations}
iterations: randomly partition the producers (columns of
\code{signal_matrix}) into two roughly-equal halves, average each half
to get two group vectors, and compute the Pearson correlation.

Use this to answer "would I get the same group CI if I'd run a
different half of my participants?".

For odd N, one randomly-chosen producer is dropped per
permutation (re-drawn each iteration, not fixed) so both halves
have \code{floor(N/2)} producers.

Reports both the mean per-permutation \code{r} and the Spearman-Brown
corrected reliability \verb{r_SB = (2 r) / (1 + r)}. 95\% CI is the
2.5 / 97.5 percentile of the permutation distribution.
}
\section{Reading the result}{

\itemize{
\item \verb{$r_hh}, mean per-permutation Pearson r between the two halves.
\item \verb{$r_sb}, Spearman-Brown projected reliability of the full
sample. This is usually the headline number to report.
\item \verb{$ci_95}, \verb{$ci_95_sb}, percentile 95\% CIs on each.
\item \verb{$distribution}, the full per-permutation r vector for plotting
or custom CIs.
\item \verb{$null} (character), the null mode used.
\item \verb{$null_distribution}, when \code{null != "none"}: numeric vector of
per-iteration \code{r_hh} under the chosen null. \code{NULL} otherwise.
\item \verb{$r_hh_null_p95}, 95th percentile of the null distribution.
\code{NA_real_} when \code{null = "none"}.
\item \verb{$r_hh_excess}, observed \code{r_hh} minus the null median.
\item \verb{$r_sb_excess}, the same excess applied to Spearman-Brown
projected \code{r_sb}.
\item \verb{$n_participants}, \verb{$n_permutations}, metadata.
}
}

\section{Common mistakes}{

\itemize{
\item Reporting \verb{$r_hh} instead of \verb{$r_sb} when the audience cares
about the full-sample CI's reliability (almost always).
\item Running with a small \code{n_permutations} (< 200) to save time and
then trusting the CI bounds; CIs widen with low n_perm.
}
}

\section{Reliability metrics expect raw masks}{

Operates on the raw mask; results may be distorted if
\code{signal_matrix} was extracted from rendered (scaled) PNGs. See
the rcisignal output-reliability vignette chapter on raw vs.
rendered.
}

\examples{
\dontrun{
# Minimal call-signature demo with a synthetic input. Split-half
# correlation will hover near zero because the input is pure noise.
n_pix  <- 32L * 32L
n_prod <- 20L
set.seed(1)
signal_matrix <- matrix(rnorm(n_pix * n_prod), n_pix, n_prod)
rel_split_half(signal_matrix, n_permutations = 200L, seed = 1)
}

\dontrun{
# Same function, richer input: plant a strong signal in the eye region.
# Split-half correlation should now be measurably positive.
sim <- simulate_briefrc_data(
  n_per_condition = 20, n_trials = 60, conditions = "target",
  signal_region = "eyes", signal_strength = "strong", seed = 1
)
cis <- ci_from_responses_briefrc(sim$data, noise_matrix = sim$noise_matrix)
rel_split_half(cis$signal_matrix, n_permutations = 200L, seed = 1)
}
}
\references{
Brinkman, L., Todorov, A., & Dotsch, R. (2017). Visualising
mental representations: A primer on noise-based reverse
correlation in social psychology. \emph{European Review of Social
Psychology}, 28(1), 333-361.
\doi{10.1080/10463283.2017.1381469}

Shrout, P. E., & Fleiss, J. L. (1979). Intraclass correlations:
uses in assessing rater reliability. \emph{Psychological Bulletin},
86(2), 420-428. \doi{10.1037/0033-2909.86.2.420}
}
\seealso{
\code{\link[=rel_loo]{rel_loo()}}, \code{\link[=rel_icc]{rel_icc()}}, \code{\link[=run_reliability]{run_reliability()}}
}
