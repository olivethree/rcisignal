% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rel_cluster_test.R
\name{rel_cluster_test}
\alias{rel_cluster_test}
\title{Cluster-based permutation test with family-wise error control}
\usage{
rel_cluster_test(
  signal_matrix_a,
  signal_matrix_b,
  img_dims = NULL,
  method = c("threshold", "tfce"),
  paired = FALSE,
  n_permutations = 2000L,
  cluster_threshold = 2,
  tfce_H = 2,
  tfce_E = 0.5,
  tfce_n_steps = 100L,
  alpha = 0.05,
  mask = NULL,
  seed = NULL,
  progress = TRUE,
  acknowledge_scaling = FALSE
)
}
\arguments{
\item{signal_matrix_a, signal_matrix_b}{Pixels x participants,
base-subtracted.}

\item{img_dims}{Integer \code{c(nrow, ncol)}. Inferred from
\code{attr(signal_matrix_a, "img_dims")} when \code{NULL}.}

\item{method}{One of \code{"threshold"} (default) or \code{"tfce"}.}

\item{paired}{Logical. \code{FALSE} (default) for between-subjects:
Welch t and stratified label permutation. \code{TRUE} for
within-subjects: paired t on \code{A - B} and random sign-flip of
matched pairs for the null.}

\item{n_permutations}{Default 2000.}

\item{cluster_threshold}{Absolute t-value threshold for forming
clusters under \code{method = "threshold"}. Default 2.0.}

\item{tfce_H, tfce_E}{TFCE height and extent exponents.
Defaults match Smith & Nichols (2009): 2.0 and 0.5.}

\item{tfce_n_steps}{Number of thresholds in the TFCE
integration grid. Default 100.}

\item{alpha}{Significance level. Default 0.05.}

\item{mask}{Optional logical vector of length \code{n_pixels}
(column-major) restricting cluster / TFCE inference to a
region. The implementation uses a zero-out pattern (not
row-subsetting) so the 2D image structure is preserved for
4-connectivity. Build with \code{\link[=make_face_mask]{make_face_mask()}} (parametric
oval and sub-regions) or \code{\link[=read_face_mask]{read_face_mask()}} (PNG/JPEG mask).}

\item{seed}{Optional integer; RNG state restored on exit.}

\item{progress}{Show a \code{cli} progress bar.}

\item{acknowledge_scaling}{Logical. When \code{FALSE} (default), the
internal \code{pixel_t_test()} errors on a known-rendered matrix.}
}
\value{
Object of class \code{rcisignal_rel_cluster_test}.
}
\description{
Pixel-level discriminability test between two condition signal
matrices. Returns either (a) the classical threshold-based
cluster test (Maris & Oostenveld, 2007) with cluster mass as the
statistic, or (b) threshold-free cluster enhancement (TFCE;
Smith & Nichols, 2009), which integrates across thresholds and
removes the arbitrary cluster-forming cutoff. Both paths use the
same stratified permutation scheme and the maximum-statistic
approach to family-wise error control (Nichols & Holmes, 2002).

Pair with \code{\link[=rel_dissimilarity]{rel_dissimilarity()}} when you also want an overall
magnitude summary.
}
\details{
Common scaffold (both methods):
\enumerate{
\item Compute observed pixel-wise Welch t (see \code{\link[=pixel_t_test]{pixel_t_test()}}).
\item Build the null via \code{n_permutations} stratified label
permutations, each preserving \verb{(N_A, N_B)} exactly.
\item Max-statistic over the null controls FWER for the family of
clusters (the probability of any false-positive cluster under
H0 is bounded by \code{alpha}). Pixel-level localization within a
significant cluster is only weakly controlled
(Maris & Oostenveld, 2007, §4.4.3); use \code{\link[=agreement_map_test]{agreement_map_test()}}
when pixel-level FWER control is needed.
}

Method-specific step:
\itemize{
\item \code{method = "threshold"} (default): threshold at
\verb{|t| > cluster_threshold}, label connected components with
4-connectivity, cluster mass = sum of t-values within the
cluster.
\item \code{method = "tfce"}: enhance the observed t-map (and each
permutation t-map) into a TFCE map; record \verb{max(|TFCE|)} per
permutation. Per-pixel p-value = fraction of null max-TFCE
that equals or exceeds the observed \verb{|TFCE|}. No free
threshold parameter.
}
}
\section{Reading the result (threshold method)}{

\itemize{
\item \verb{$observed_t}, per-pixel Welch t vector.
\item \verb{$clusters}, data.frame with \code{cluster_id}, \code{direction},
\code{mass}, \code{size}, \code{p_value}, \code{significant}.
\item \verb{$null_distribution$pos}, \verb{$neg}, per-permutation max masses.
\item \verb{$pos_labels}, \verb{$neg_labels}, integer matrices for plotting.
\item \verb{$cluster_threshold}, \verb{$alpha}, \verb{$n_permutations},
\verb{$n_participants_a}, \verb{$n_participants_b},
\verb{$method = "threshold"}.
}
}

\section{Reading the result (TFCE method)}{

\itemize{
\item \verb{$observed_t}, per-pixel Welch t vector (before enhancement).
\item \verb{$tfce_map}, per-pixel signed TFCE values.
\item \verb{$tfce_pmap}, per-pixel p-values against the max-TFCE null.
\item \verb{$tfce_significant_mask}, logical vector flagging pixels with
\code{p < alpha}.
\item \verb{$null_distribution$max_abs_tfce}, per-permutation max|TFCE|.
\item \verb{$tfce_H}, \verb{$tfce_E}, \verb{$tfce_n_steps}, \verb{$alpha},
\verb{$n_permutations}, \verb{$n_participants_a}, \verb{$n_participants_b},
\verb{$method = "tfce"}.
}
}

\section{Reading the plot}{

\code{plot()} on the returned object renders the per-pixel observed
Welch t (or signed TFCE values for \code{method = "tfce"}) as a
raster image with FWE-significant cluster boundaries traced on
top.
\itemize{
\item \strong{Color} encodes the sign and magnitude of the per-pixel
statistic. Blue = condition A larger than B at that pixel
(positive Welch t); red = condition B larger than A
(negative Welch t); white = near zero. The colorbar reads
in \verb{Welch t} units (or signed \verb{TFCE value} for TFCE).
\item \strong{Black contours} trace the boundary of clusters that are
significant at the chosen \code{alpha} under the max-mass null
(threshold method) or the boundary of pixels with FWE-
corrected \code{p < alpha} (TFCE method). Pixels inside the
contour survive the FWE correction; pixels outside do not.
\item The color scale is symmetric around zero by default so the
neutral color aligns with \code{t = 0}. The displayed t-map is
the \emph{observed} statistic before any thresholding; the
contours encode the inferential decision.
\item Color convention matches \code{\link[=plot_agreement_map]{plot_agreement_map()}} and
\code{\link[=plot_ci_overlay]{plot_ci_overlay()}} so the same data reads consistently
across the package.
\item \code{plot()} accepts \verb{main =} (title) and \verb{color_bar =} (logical,
default \code{TRUE}). Set \code{color_bar = FALSE} when packing many
panels into a small grid — used internally by
\code{\link[=plot.rcisignal_rel_pairwise_report]{plot.rcisignal_rel_pairwise_report()}} for that reason.
}
}

\section{Reliability metrics expect raw masks}{

Welch t and cluster mass / TFCE are variance-based and
sensitive to any scaling. Inputs with
\code{attr(., "source") == "rendered"} (set automatically by Mode 1
readers like \code{\link[=extract_signal]{extract_signal()}}) error unless
\code{acknowledge_scaling = TRUE} cascades through the internal
\code{\link[=pixel_t_test]{pixel_t_test()}} call.
}

\examples{
\dontrun{
# Minimal call-signature demo with two synthetic inputs.
n_side <- 32L
n_pix  <- n_side * n_side
n_prod <- 20L
set.seed(1)
signal_matrix_a <- matrix(rnorm(n_pix * n_prod), n_pix, n_prod)
signal_matrix_b <- matrix(rnorm(n_pix * n_prod), n_pix, n_prod)
rel_cluster_test(signal_matrix_a, signal_matrix_b,
                 img_dims = c(n_side, n_side),
                 n_permutations = 200L, seed = 1)
}

\dontrun{
# Same function, richer input: signal planted in different face regions
# (eyes vs mouth). The cluster test should find significant clusters
# at both regions, with opposite signs.
sim_eyes  <- simulate_briefrc_data(
  n_per_condition = 20, n_trials = 60, conditions = "x",
  signal_region = "eyes", signal_strength = "strong", seed = 1
)
sim_mouth <- simulate_briefrc_data(
  n_per_condition = 20, n_trials = 60, conditions = "x",
  signal_region = "mouth", signal_strength = "strong", seed = 2
)
sig_eyes  <- ci_from_responses_briefrc(
  sim_eyes$data, noise_matrix = sim_eyes$noise_matrix)$signal_matrix
sig_mouth <- ci_from_responses_briefrc(
  sim_mouth$data, noise_matrix = sim_mouth$noise_matrix)$signal_matrix
ct <- rel_cluster_test(sig_eyes, sig_mouth,
                       n_permutations = 500L, seed = 1)
# Plot the t-map with significant cluster boundaries: blue = eyes
# condition larger; red = mouth condition larger; black contours =
# FWE-significant clusters at alpha.
plot(ct, main = "Eyes vs Mouth (cluster-FWE)")
}
}
\references{
Maris, E., & Oostenveld, R. (2007). Nonparametric statistical
testing of EEG- and MEG-data. \emph{Journal of Neuroscience Methods},
164(1), 177-190. \doi{10.1016/j.jneumeth.2007.03.024}

Nichols, T. E., & Holmes, A. P. (2002). Nonparametric
permutation tests for functional neuroimaging: a primer with
examples. \emph{Human Brain Mapping}, 15(1), 1-25.
\doi{10.1002/hbm.1058}

Smith, S. M., & Nichols, T. E. (2009). Threshold-free cluster
enhancement: addressing problems of smoothing, threshold
dependence and localisation in cluster inference. \emph{NeuroImage},
44(1), 83-98. \doi{10.1016/j.neuroimage.2008.03.061}
}
\seealso{
\code{\link[=rel_dissimilarity]{rel_dissimilarity()}}, \code{\link[=run_discriminability]{run_discriminability()}}
}
