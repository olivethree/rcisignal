% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_discriminability_pairwise.R
\name{run_discriminability_pairwise}
\alias{run_discriminability_pairwise}
\title{Run all pairwise between-condition comparisons across K conditions}
\usage{
run_discriminability_pairwise(
  signal_matrices,
  fwer = c("holm", "bonferroni", "none"),
  img_dims = NULL,
  paired = FALSE,
  method = c("threshold", "tfce"),
  n_permutations = 2000L,
  n_boot = 2000L,
  cluster_threshold = 2,
  alpha = 0.05,
  ci_level = 0.95,
  dissim_null = c("none", "permutation"),
  mask = NULL,
  seed = NULL,
  progress = TRUE,
  acknowledge_scaling = FALSE
)
}
\arguments{
\item{signal_matrices}{Named list of pixels x participants
signal matrices, one per condition. Names become condition
labels in the output.}

\item{fwer}{One of \code{"holm"} (default), \code{"bonferroni"}, or
\code{"none"}.}

\item{img_dims}{Integer \code{c(nrow, ncol)}. If \code{NULL}, inferred
from \code{attr(signal_matrices[[1]], "img_dims")}.}

\item{paired}{Logical. When \code{TRUE}, all pairs use the paired
variant. All matrices must have identical column names.}

\item{method}{Cluster-test method. Default \code{"threshold"}.}

\item{n_permutations, n_boot, cluster_threshold, alpha, ci_level}{Forwarded to per-pair calls.}

\item{dissim_null}{Forwarded to \code{\link[=rel_dissimilarity]{rel_dissimilarity()}} as
\code{null}. Default \code{"none"} to keep wall time bounded.}

\item{mask}{Optional logical vector forwarded to both per-pair
calls.}

\item{seed}{Optional integer.}

\item{progress}{Show \code{cli} progress bars per per-pair call.}

\item{acknowledge_scaling}{Logical. Forwarded.}
}
\value{
Object of class \code{rcisignal_rel_pairwise_report} with:
\itemize{
\item \verb{$pairs}, data.frame: \code{pair_id}, \code{cond_a}, \code{cond_b},
\code{n_clusters}, \code{p_min}, \code{p_adj_pair}, \code{significant},
\code{euclidean}, \code{euclidean_normalised}.
\item \verb{$results}, named list of per-pair \code{rel_cluster_test} and
\code{rel_dissimilarity} results.
\item \verb{$conditions}, the input names.
\item \verb{$fwer}, the correction method used.
\item \verb{$alpha}, the across-pairs alpha.
}
}
\description{
Generalises \code{\link[=run_discriminability]{run_discriminability()}} from a 2-condition
comparison to K conditions: runs \code{\link[=rel_cluster_test]{rel_cluster_test()}} and
\code{\link[=rel_dissimilarity]{rel_dissimilarity()}} on every K-choose-2 pair and applies a
family-wise error correction across pairs.
}
\section{FWER scope}{

Cluster-level p-values within each pair are already
max-statistic FWER-controlled by \code{\link[=rel_cluster_test]{rel_cluster_test()}}. The
Holm/Bonferroni adjustment here controls family-wise error
across the K-choose-2 pair comparisons (a second layer above
the cluster test's internal control), not over individual
pixels or clusters.

For each pair, the statistic carried into the across-pairs
adjustment is the minimum cluster-level p-value within that
pair. Within-pair cluster p-values are not re-adjusted; they
remain the max-statistic FWER-controlled values from
\code{rel_cluster_test()}. A pair with no clusters contributes
\code{p_min = 1.0} so the Holm ordering is well-defined.
}

\examples{
\dontrun{
# Three-condition comparison: simulate -> per-condition CIs -> all pairs.
sim <- simulate_briefrc_data(n_per_condition = 10, n_trials = 60,
                             conditions = c("happy", "angry", "neutral"),
                             seed = 1)
sigs <- lapply(split(sim$data, sim$data$condition), function(d) {
  ci_from_responses_briefrc(d, noise_matrix = sim$noise_matrix)$signal_matrix
})
run_discriminability_pairwise(sigs,
                              n_permutations = 200L, n_boot = 200L,
                              seed = 1)
}
}
\seealso{
\code{\link[=run_discriminability]{run_discriminability()}}, \code{\link[=rel_cluster_test]{rel_cluster_test()}},
\code{\link[=rel_dissimilarity]{rel_dissimilarity()}}
}
