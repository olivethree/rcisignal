% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ci_io.R
\name{read_signal_matrix}
\alias{read_signal_matrix}
\title{Read a directory of CI images and extract the base-subtracted signal}
\usage{
read_signal_matrix(
  dir,
  base_image_path,
  pattern = "\\\\.(png|jpe?g)$",
  acknowledge_scaling = FALSE
)
}
\arguments{
\item{dir}{Directory of CI images (as for \code{\link[=read_cis]{read_cis()}}).}

\item{base_image_path}{Path to the base face image used at
stimulus-generation time.}

\item{pattern}{Regex for file selection (see \code{\link[=read_cis]{read_cis()}}).}

\item{acknowledge_scaling}{Set to \code{TRUE} to silence the
once-per-session warning. See \code{\link[=read_cis]{read_cis()}}.}
}
\value{
A numeric pixels x participants matrix, base-subtracted.
}
\description{
Convenience wrapper composing \code{\link[=read_cis]{read_cis()}} and \code{\link[=extract_signal]{extract_signal()}}.
Returns a pixels x participants signal matrix in one call. Use
this when you don't need to intervene between reading and
extracting (e.g. masking pixels, swapping the base).

Note that the \emph{signal matrix} (this function's output) and the
\emph{noise matrix} (\code{\link[=read_noise_matrix]{read_noise_matrix()}}) are different objects:
the signal matrix is \verb{pixels x participants}, one column per
producer's CI; the noise matrix is \verb{pixels x pool_size}, the
basis noise patterns from stimulus generation.
}
\section{Reading the result}{

Pixels x participants numeric matrix, base-subtracted. Carries an
\code{img_dims} attribute. Column names are the filename without the
extension. Tagged with \code{attr(., "source") = "rendered"} because
PNG-derived signals are scaled, not raw.
}

\section{Common mistakes}{

\itemize{
\item Pointing \code{base_image_path} at the wrong file (e.g. a generated
CI PNG instead of the base face used at stimulus-generation
time). The dimension check catches a wholly wrong image but
not a wrong-base-of-the-right-size.
\item Treating the result as raw mask. See note below.
}
}

\section{Raw vs. rendered CIs}{

PNG pixel values are necessarily what was rendered to disk:
\code{base + scaling(mask)}. Subtracting the base in \code{\link[=extract_signal]{extract_signal()}}
therefore yields \code{scaling(mask)}, not the raw mask. Variance-based
reliability metrics (\code{\link[=rel_icc]{rel_icc()}}, Euclidean half of
\code{\link[=rel_dissimilarity]{rel_dissimilarity()}}, \code{\link[=pixel_t_test]{pixel_t_test()}}, \code{\link[=rel_cluster_test]{rel_cluster_test()}}) are
sensitive to the scaling transform; only correlation-based metrics
(\code{\link[=rel_split_half]{rel_split_half()}}, \code{\link[=rel_loo]{rel_loo()}}) survive a single uniform linear
scaling unmodified, and even those break under per-CI
\code{"matched"}-style scaling. The standard 2IFC infoVal path
(\code{rcicr::computeInfoVal2IFC()}) extracts the raw \verb{$ci} internally
from the rcicr CI-list and is \strong{not} affected; hand-rolled
\code{infoVal} implementations (Brief-RC, custom code) \strong{are}, and
should be fed the raw mask. Prefer \code{\link[=ci_from_responses_2ifc]{ci_from_responses_2ifc()}} /
\code{\link[=ci_from_responses_briefrc]{ci_from_responses_briefrc()}} when raw responses are available;
both return the raw mask.
}

\examples{
\dontrun{
signal <- read_signal_matrix(
  dir             = "data/cis_condition_A/",
  base_image_path = "data/base.jpg"
)
}
}
\seealso{
\code{\link[=read_cis]{read_cis()}}, \code{\link[=extract_signal]{extract_signal()}}, \code{\link[=read_noise_matrix]{read_noise_matrix()}}
}
