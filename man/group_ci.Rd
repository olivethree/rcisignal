% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group_ci.R
\name{group_ci}
\alias{group_ci}
\title{Collapse a per-producer signal matrix into per-group means}
\usage{
group_ci(
  signal_matrix,
  responses,
  by,
  col_participant = "participant_id",
  drop = TRUE
)
}
\arguments{
\item{signal_matrix}{Numeric matrix of pixels x n_producers, as
returned by \code{\link[=ci_from_responses_briefrc]{ci_from_responses_briefrc()}} or
\code{\link[=ci_from_responses_2ifc]{ci_from_responses_2ifc()}} in their \verb{$signal_matrix} field.
Must have non-empty column names (the producer ids that map
into \code{responses[[col_participant]]}).}

\item{responses}{Trial-level data frame containing one row per
trial. Must contain the column named by \code{col_participant}
(mapping to \code{colnames(signal_matrix)}) and every column
named in \code{by}. Each producer's \code{by} value(s) must be
consistent across all of their rows; an inconsistency aborts
with a teaching message naming the offending producer.}

\item{by}{Character vector of column names in \code{responses}.
Length 1 selects a single grouping column (e.g.
\code{by = "condition"}). Length 2+ produces a factorial grouping
where cell labels are the levels joined with \code{"_"} in the
given order (e.g. \code{by = c("condition", "sex")} yields cells
like \code{"happy_F"}).}

\item{col_participant}{Name of the participant-id column in
\code{responses}. Default \code{"participant_id"}, matching the rest
of the package.}

\item{drop}{Logical. When \code{TRUE} (default), empty cells are
dropped from the output. When \code{FALSE}, empty cells are
present as \code{NA} columns with \code{n = 0L}.}
}
\value{
A numeric matrix of pixels x n_groups. Carries:
\itemize{
\item column names = group labels;
\item \code{attr(., "n")} = named integer vector of per-group producer
counts;
\item \code{attr(., "img_dims")} = inherited from
\code{attr(signal_matrix, "img_dims")} if present;
\item \code{attr(., "by_name")} = text description of the grouping;
\item \code{attr(., "ci_level") = "group"} = a one-string hint used by
\code{\link[=save_ci_images]{save_ci_images()}} to pick the filename prefix. Not a class;
no S3 dispatch attached.
}
}
\description{
Convenience helper that takes a per-producer \code{signal_matrix}
(pixels x n_producers, the object returned by
\code{\link[=ci_from_responses_briefrc]{ci_from_responses_briefrc()}} / \code{\link[=ci_from_responses_2ifc]{ci_from_responses_2ifc()}}) and
a trial-level \code{responses} data frame, and collapses producers into
per-group means via \code{\link[=rowMeans]{rowMeans()}}.

Uses the same "data frame plus column name" idiom as every other
responses-consuming function in the package: pass \code{responses} plus
the name of the column(s) you want to group by. Producer-to-group
alignment happens internally via \code{colnames(signal_matrix)}.

\verb{ci_from_responses_*()} will call this for you if you pass them a
\verb{group_by =} argument, so most users do not need to call
\code{group_ci()} directly. Reach for it when you want to build group
CIs from a signal matrix you already have in hand (e.g. one read
back from disk).
}
\details{
For each group, the corresponding output column is
\code{rowMeans(signal_matrix[, producers_in_group, drop = FALSE])}. For a
factorial \code{by} (length 2+), the cell label is the levels joined by
\code{"_"} in the column order given.
}
\examples{
\dontrun{
sim <- simulate_briefrc_data(
  n_per_condition = 10, n_trials = 60,
  conditions = c("A", "B"), seed = 1
)
cis  <- ci_from_responses_briefrc(sim$data,
                                  noise_matrix = sim$noise_matrix)
gcis <- group_ci(cis$signal_matrix, sim$data, by = "condition")
gcis                              # n_pixels x 2 (A and B)
attr(gcis, "n")                   # per-group producer counts

# Or skip the standalone call: pass group_by to the generator and
# get both per-producer and group matrices in one shot.
both <- ci_from_responses_briefrc(sim$data,
                                  noise_matrix = sim$noise_matrix,
                                  group_by     = "condition")
identical(both$group_ci, gcis)
}
}
\seealso{
\code{\link[=ci_from_responses_briefrc]{ci_from_responses_briefrc()}},
\code{\link[=ci_from_responses_2ifc]{ci_from_responses_2ifc()}} (their \verb{group_by =} argument calls
this internally); \code{\link[=plot_ci_distance_matrix]{plot_ci_distance_matrix()}}, \code{\link[=plot_ci_mds]{plot_ci_mds()}},
\code{\link[=plot_ci_correlogram]{plot_ci_correlogram()}} (compare multiple CIs, accept either
per-producer or group-level matrices); \code{\link[=save_ci_images]{save_ci_images()}}.
}
