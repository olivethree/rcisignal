% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_face_mask.R
\name{plot_face_mask}
\alias{plot_face_mask}
\title{Plot a face mask for visual verification}
\usage{
plot_face_mask(
  mask = NULL,
  img_dims = NULL,
  base_image = NULL,
  alpha = 0.5,
  col = "red",
  threshold = 0.5,
  main = NULL,
  region = NULL,
  region_bounds = NULL,
  ...
)
}
\arguments{
\item{mask}{One of: a logical or numeric vector of length
\code{prod(img_dims)} (column-major, as returned by \code{\link[=make_face_mask]{make_face_mask()}} or
\code{\link[=read_face_mask]{read_face_mask()}}); a logical or numeric matrix; or a path to
a PNG/JPEG mask file. Pass \code{NULL} and a \code{region} to build the
mask internally via \code{\link[=make_face_mask]{make_face_mask()}}.}

\item{img_dims}{Integer \code{c(nrow, ncol)}, or a single integer for a
square image. Required when \code{mask} is a vector or when
\code{region} is supplied; ignored otherwise.}

\item{base_image}{Optional path to a PNG or JPEG base face. When
supplied, the mask is rendered as a translucent overlay on top.}

\item{alpha}{Numeric in \verb{[0, 1]}. Overlay opacity. Default \code{0.5}.}

\item{col}{Highlight color for the masked region. Default
\code{"red"}.}

\item{threshold}{When \code{mask} is a numeric matrix or image path,
pixels strictly above this value are treated as inside the
mask. Default \code{0.5}. Ignored for logical input.}

\item{main}{Optional plot title.}

\item{region}{Optional character region name (one of the
\code{\link[=make_face_mask]{make_face_mask()}} choices) used as a convenience shortcut:
builds the mask internally via
\code{make_face_mask(img_dims, region, region_bounds)}. Mutually
exclusive with \code{mask}.}

\item{region_bounds}{Optional length-4 numeric vector forwarded
to \code{\link[=make_face_mask]{make_face_mask()}} when \code{region} is one of the rectangle
regions. Ignored otherwise.}

\item{...}{Reserved for future use.}
}
\value{
Invisibly returns the resolved logical matrix
(\code{nrow} x \code{ncol}, top-left origin).
}
\description{
Renders a face mask so you can confirm it covers the region you
intended before passing it to \code{\link[=infoval_report]{infoval_report()}} or \code{\link[=infoval]{infoval()}}.
Accepts the same input forms the diagnostics accept: a logical or
numeric vector (column-major, with \code{img_dims} supplied), a
logical/numeric matrix, or a path to a PNG/JPEG mask file.
}
\details{
If \code{base_image} is supplied, the mask is drawn as a translucent
overlay on top of the base face — the most useful view for
verifying that the mask aligns with eyes, nose, mouth, etc. The
base image must have the same dimensions as the mask; otherwise
the base is dropped with a warning.
}
\examples{
m <- make_face_mask(c(128L, 128L), region = "eyes")
plot_face_mask(m, img_dims = c(128L, 128L), main = "eyes region")

}
\seealso{
\code{\link[=plot_mask_overlay]{plot_mask_overlay()}} (overlay on a specific base image),
\code{\link[=make_face_mask]{make_face_mask()}}, \code{\link[=read_face_mask]{read_face_mask()}}, \code{\link[=infoval_report]{infoval_report()}}.
}
