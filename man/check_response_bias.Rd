% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_response_bias.R
\name{check_response_bias}
\alias{check_response_bias}
\title{Check for response bias}
\usage{
check_response_bias(
  responses,
  method = c("2ifc", "briefrc"),
  col_participant = "participant_id",
  col_response = "response",
  bias_threshold = 0.6,
  ...
)
}
\arguments{
\item{responses}{A data frame of trial-level responses.}

\item{method}{Either \code{"2ifc"} or \code{"briefrc"}.}

\item{col_participant, col_response}{Column names.}

\item{bias_threshold}{Numeric. Participants with absolute mean response
exceeding this value are flagged. Default \code{0.6}, which for \verb{\{-1, 1\}}
coding corresponds to approximately 80\% of responses in one direction.}

\item{...}{Unused.}
}
\value{
An \code{\link[=rcisignal_diag_result]{rcisignal_diag_result()}} object. The \code{data} element contains
\code{per_participant}, a data.table with participant id, trial count,
mean response, flag for constant response, and flag for extreme mean.
}
\description{
Flags two kinds of response bias:
}
\details{
\enumerate{
\item Per-participant constant responding: a participant who gives the same
response on every trial. This is almost always a serious problem
(disengaged participant or a coding bug) and produces a status of
\code{"fail"} if any such participant is present.
\item Per-participant extreme mean: a participant whose mean response is
further from zero than \code{bias_threshold}. For binary \verb{\{-1, 1\}} coding,
the default threshold of \code{0.6} corresponds to roughly an 80/20 split.
Status is \code{"warn"} if any extreme participants are found and no
constant responders are present.
}

The absolute-threshold approach is more robust than MAD-scaled outlier
detection when the group is small, because MAD collapses when most
participants respond similarly.
}
\examples{
set.seed(1)
responses <- data.frame(
  participant_id = rep(1:4, each = 100),
  stimulus       = rep(1:100, 4),
  response       = c(
    sample(c(-1, 1), 100, replace = TRUE),
    sample(c(-1, 1), 100, replace = TRUE),
    rep(1, 100),
    sample(c(-1, 1), 100, replace = TRUE)
  )
)
check_response_bias(responses)

}
