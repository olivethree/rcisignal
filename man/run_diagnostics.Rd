% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_diagnostics.R
\name{run_diagnostics}
\alias{run_diagnostics}
\title{Run the full battery of diagnostic checks}
\usage{
run_diagnostics(
  responses,
  method = NULL,
  rdata = NULL,
  stimuli = NULL,
  noise_matrix = NULL,
  baseimage = "base",
  expected_n = NULL,
  col_participant = "participant_id",
  col_stimulus = "stimulus",
  col_response = "response",
  col_rt = NULL,
  infoval_iter = NULL,
  face_mask = "auto",
  with_replacement = "auto",
  ...
)
}
\arguments{
\item{responses}{Data frame with one row per trial. Required
columns: \code{participant_id}, \code{stimulus}, \code{response} (values in
\verb{\{-1, +1\}}); optional \code{rt} for the RT diagnostics. Load yours
from CSV via \code{\link[=read_responses]{read_responses()}} or \code{\link[utils:read.table]{utils::read.csv()}}; column
names are configurable via the \verb{col_*} arguments.}

\item{method}{\code{"2ifc"} or \code{"briefrc"}, or \code{NULL} to auto-detect.}

\item{rdata}{Optional. Path to an rcicr \code{.RData} file. Enables
\code{\link[=check_stimulus_alignment]{check_stimulus_alignment()}}, \code{\link[=check_version_compat]{check_version_compat()}} (2IFC),
\code{\link[=compute_infoval_summary]{compute_infoval_summary()}}, \code{\link[=check_response_inversion]{check_response_inversion()}}, and
\code{\link[=check_rt_infoval_consistency]{check_rt_infoval_consistency()}}. Mutually exclusive
alternative to \code{stimuli}.}

\item{stimuli}{Optional in-memory stimuli list (\verb{$stimuli} from
an \code{rcisignal_sim} object). Same downstream effect as
\code{rdata} but session-portable (use after \code{\link[=readRDS]{readRDS()}} when the
path stored on \verb{$rdata_path} no longer resolves).}

\item{noise_matrix}{Optional. Path to a Brief-RC noise-matrix text
file, or an already-loaded numeric matrix. Enables
\code{\link[=validate_noise_matrix]{validate_noise_matrix()}} and \code{\link[=check_stimulus_alignment]{check_stimulus_alignment()}} (Brief-RC).}

\item{baseimage}{Name of the base image used at stimulus-generation
time (key in \code{base_face_files} in the rdata). Default \code{"base"}.
Only consulted when the infoval-dependent checks run.}

\item{expected_n}{Optional. Passed to \code{\link[=check_trial_counts]{check_trial_counts()}}.}

\item{col_participant, col_stimulus, col_response, col_rt}{Column-name
overrides.}

\item{infoval_iter}{Number of iterations for rcicr's reference
distribution; \code{NULL} disables infoVal checks even if \code{rdata} is
supplied. Default \code{NULL} because the reference simulation is slow
and unwanted by default; set e.g. \code{10000} to enable.}

\item{face_mask}{Mask spec passed to \code{\link[=diagnose_infoval]{diagnose_infoval()}}. Default
\code{"auto"} (Schmitz 2024 oval). Pass \code{NULL} to skip the masked-vs-
unmasked comparison, or any value \code{\link[=diagnose_infoval]{diagnose_infoval()}} accepts.}

\item{with_replacement}{Sampling regime forwarded to
\code{\link[=diagnose_infoval]{diagnose_infoval()}} / \code{\link[=infoval]{infoval()}} for the across-trials reference
distribution. Default \code{"auto"} matches the standard Brief-RC
convention.}

\item{...}{Unused.}
}
\value{
An object of class \code{rcisignal_diag_report}.
}
\description{
Executes every implemented check and returns a \code{\link[=rcisignal_diag_report]{rcisignal_diag_report()}}
collecting the results. Checks whose required inputs are missing are
skipped gracefully and listed in the report's \verb{$skipped_checks}
element.
}
\details{
The orchestrator auto-detects the method when only \code{rdata} or only
\code{noise_matrix} is supplied; otherwise pass \code{method} explicitly.
}
\examples{
responses <- data.frame(
  participant_id = rep(1:3, each = 100),
  stimulus       = rep(1:100, 3),
  response       = sample(c(-1, 1), 300, replace = TRUE),
  rt             = round(exp(rnorm(300, 6.7, 0.4)) + 100, 1)
)
report <- run_diagnostics(responses, method = "2ifc", col_rt = "rt")
print(report)

}
