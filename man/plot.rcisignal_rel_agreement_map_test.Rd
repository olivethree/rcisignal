% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agreement_map_test.R
\name{plot.rcisignal_rel_agreement_map_test}
\alias{plot.rcisignal_rel_agreement_map_test}
\title{Plot an agreement-map test result}
\usage{
\method{plot}{rcisignal_rel_agreement_map_test}(
  x,
  palette = c("diverging", "fire"),
  threshold = NULL,
  zlim = NULL,
  base_image = NULL,
  alpha_max = 0.7,
  show_contour = TRUE,
  contour_col = "black",
  contour_lwd = 1,
  main = "Agreement t-map (FWE contours)",
  ...
)
}
\arguments{
\item{x}{A \code{\link[=agreement_map_test]{agreement_map_test()}} result.}

\item{palette}{\code{"diverging"} (default; signed t, blue =
positive, red = negative) or \code{"fire"} (\verb{|t|} on a single-hue
ramp). See \code{\link[=plot_agreement_map]{plot_agreement_map()}} for the full Reading-the-plot
discussion; the same conventions apply here.}

\item{threshold}{Optional positive numeric. Pixels with
\verb{|t| < threshold} render as the neutral colour (descriptive
only; FWE control is already in \code{significant_mask}).}

\item{zlim}{Numeric \code{c(low, high)} for the colour scale.
Defaults to a symmetric \verb{c(-max|t|, max|t|)} for diverging or
\verb{c(0, max|t|)} for fire.}

\item{base_image}{Optional. Numeric matrix or path to PNG/JPEG.
When supplied, the t-map is composited on top of the grayscale
base; out-of-mask and subthreshold pixels render fully
transparent.}

\item{alpha_max}{Numeric in \verb{[0, 1]}. Maximum opacity at the
colour-scale top when \code{base_image} is supplied. Default 0.7.}

\item{show_contour}{Logical. Draw the FWE-significant pixel
boundary as black contours on top of the t-map. Default \code{TRUE}.}

\item{contour_col, contour_lwd}{Significance-contour colour and
line width.}

\item{main}{Plot title.}

\item{...}{Reserved for future use.}
}
\value{
Invisibly the input \code{x}.
}
\description{
Renders the observed per-pixel t-map from \code{\link[=agreement_map_test]{agreement_map_test()}}
with the same colour conventions as \code{\link[=plot_agreement_map]{plot_agreement_map()}}, and
overlays the FWE-significant pixel boundary as black contours
when \code{show_contour = TRUE} (the default). Optionally composites
the map on a grayscale base face.

This is the one-call form of the canonical pairing
\code{plot_agreement_map(signal, ...) + agreement_map_test(...)}:
the test object carries everything the renderer needs
(\code{observed_t}, \code{significant_mask}, \code{img_dims}), so users do not
have to re-thread the source \code{signal_matrix}.
}
\seealso{
\code{\link[=plot_agreement_map]{plot_agreement_map()}}, \code{\link[=plot_ci_overlay]{plot_ci_overlay()}},
\code{\link[=agreement_map_test]{agreement_map_test()}}.
}
