% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/face_mask.R
\name{read_face_mask}
\alias{read_face_mask}
\title{Read an image-based face-region mask from a PNG or JPEG}
\usage{
read_face_mask(path, threshold = 0.5, invert = FALSE, expected_dims = NULL)
}
\arguments{
\item{path}{Path to a PNG or JPEG mask image.}

\item{threshold}{Numeric in \verb{[0, 1]}. Pixels with luminance above
this become \code{TRUE}. Default \code{0.5} (mid-grey).}

\item{invert}{If \code{TRUE}, the mask is inverted (useful for
black-on-white masks). Default \code{FALSE}.}

\item{expected_dims}{Optional integer \code{c(nrow, ncol)}. When set,
aborts if the loaded image's dimensions do not match — useful
for catching a wrong-resolution mask before it silently corrupts
a downstream computation.}
}
\value{
Logical vector of length \code{prod(img_dims)}, column-major.
}
\description{
Reads a binary mask image from disk and returns a logical vector
of length \code{prod(img_dims)} in column-major order, the format
the rest of the package expects. White / light pixels (above
\code{threshold}) become \code{TRUE}, dark pixels become \code{FALSE}. Use this
for image-based masks created with
\href{https://debruine.github.io/webmorphR/}{webmorphR::mask_oval()},
painted in GIMP, drawn with PowerPoint shapes, or any other tool
that produces a binary PNG/JPEG.

Companion to \code{\link[=make_face_mask]{make_face_mask()}} (parametric oval). Either
function returns the same logical-vector shape.
}
\examples{
\dontrun{
fm <- read_face_mask("masks/oval_256.png",
                     expected_dims = c(256L, 256L))
iv <- infoval(signal_matrix, noise_matrix, trial_counts,
              mask = fm, iter = 1000L, seed = 1L)
}
}
\seealso{
\code{\link[=make_face_mask]{make_face_mask()}}, \code{\link[=infoval]{infoval()}}, \code{\link[=plot_face_mask]{plot_face_mask()}},
\code{\link[=plot_mask_overlay]{plot_mask_overlay()}}.
}
