% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ci_from_responses_briefrc.R
\name{ci_from_responses_briefrc}
\alias{ci_from_responses_briefrc}
\title{Compute individual Brief-RC CIs from trial-level responses}
\usage{
ci_from_responses_briefrc(
  responses,
  rdata_path = NULL,
  noise_matrix = NULL,
  base_image = NULL,
  col_participant = "participant_id",
  col_stimulus = "stimulus",
  col_response = "response",
  method = c("briefrc12", "briefrc20"),
  scaling = c("none", "matched", "constant"),
  scaling_constant = NULL,
  base_image_path = NULL
)
}
\arguments{
\item{responses}{Data frame with one row per trial. Must contain
the columns named by \code{col_participant}, \code{col_stimulus},
\code{col_response}. \code{response} values must be in \verb{\{-1, +1\}}.}

\item{rdata_path, noise_matrix}{Exactly one must be supplied.
Provide \code{rdata_path} to read the noise matrix from an rcicr
\code{.Rdata}, or pass a pre-loaded \code{noise_matrix} directly.}

\item{base_image}{Base face image. Either a numeric matrix in
\verb{[0, 1]} (e.g. \code{sim$base_face} from
\code{\link[=simulate_briefrc_data]{simulate_briefrc_data()}}, or a hand-built mask) or a single
string path to a PNG / JPEG. Optional when \code{scaling = "none"}
(default), in which case the image is not needed for the
mathematical mask. Required for the visualization-only
\verb{$rendered_ci} field when \code{scaling} is \code{"matched"} or
\code{"constant"}.}

\item{col_participant, col_stimulus, col_response}{Column names.}

\item{method}{Brief-RC variant: \code{"briefrc12"} (12 alternatives
per trial, 6 original + 6 inverted; the default) or
\code{"briefrc20"} (20 alternatives per trial, 10 original + 10
inverted). Both variants are validated in Schmitz, Rougier,
& Yzerbyt (2024). The CI computation is identical for both;
the argument is kept as metadata so that downstream code and
reports can record which paradigm produced the data.}

\item{scaling}{Visualization-only scaling for the optional
\verb{$rendered_ci} field. One of \code{"none"} (default), \code{"matched"}
(stretch mask to base range, then add to base) or
\code{"constant"} (multiply mask by \code{scaling_constant}, then add
to base). The mathematical \verb{$signal_matrix} is always the raw
unscaled mask.}

\item{scaling_constant}{Numeric multiplier used when
\code{scaling = "constant"}. Ignored otherwise.}

\item{base_image_path}{\strong{Deprecated.} Use \code{base_image} (which
accepts both a numeric matrix and a path). The old name still
works for one release with a deprecation warning.}
}
\value{
A list with \code{signal_matrix}, optionally \code{rendered_ci},
\code{participants}, \code{img_dims}, \code{scaling}, and \code{method} (the
Brief-RC variant the call was made with).
}
\description{
Native implementation of Schmitz, Rougier & Yzerbyt (2024)'s
Brief-RC mask. Does not call any \verb{rcicr::*_brief} function;
those do not exist in upstream rcicr v1.0.1. Only rcicr's
noise-pattern pool (the \code{stimuli} object inside an \code{.Rdata}
from \code{generateStimuli2IFC()}) is reused; the mask is computed
in pure R.

Use this when you have Brief-RC 12 or Brief-RC 20 trial-level
responses and want the package to produce per-producer noise
masks ready for the reliability metrics.
}
\details{
Formula (Schmitz's \code{genMask()} exactly):

\if{html}{\out{<div class="sourceCode">}}\preformatted{X    <- data.table(response, stim)
X    <- X[, .(response = mean(response)), stim]  # collapse duplicates
mask <- (noiseMatrix[, X$stim] \%*\% X$response) / length(X$response)
}\if{html}{\out{</div>}}

The \code{length(X$response)} denominator is the number of unique
pool ids chosen by that participant, not the raw trial count.
If a participant chooses the same stimulus on two trials with
opposite responses, those two cancel.

The formula is symmetric in the per-trial split (6/6 for
Brief-RC 12, 10/10 for Brief-RC 20), so the same code path
handles both variants. The \code{method} argument is recorded as
provenance metadata and validated; it does not branch the
computation.
}
\section{Reading the result}{

\itemize{
\item \verb{$signal_matrix} is the raw mask per producer; pass this and
only this to reliability metrics or any external infoVal
computation.
\item \verb{$rendered_ci}, when present, is \code{base + scaling(mask)} per
producer. \strong{Visualization only.}
\item \verb{$participants} and \verb{$img_dims} are convenience metadata.
}
}

\section{Common mistakes}{

\itemize{
\item Passing the "expanded" multi-row-per-trial response format.
Brief-RC data is \strong{one row per trial}; \code{stimulus} is the
chosen pool id and \code{response} is \code{+1} (original) or \code{-1}
(inverted). See Schmitz et al. (2024) sec. 3.1.2.
\item Using \verb{$rendered_ci} for downstream stats; it exists only
because the user often wants to save a PNG.
}
}

\examples{
\dontrun{
# In your study, you replace the simulated inputs with your own:
#   - `sim$data` -> your responses data frame (CSV via read_responses())
#   - `sim$noise_matrix` -> a numeric matrix `n_pixels x pool_size`
#     (load via read_noise_matrix() from the OSF txt that came with
#     your Brief-RC stimuli).
# The `simulate_briefrc_data()` helper just produces the same shapes so
# the example below is runnable end-to-end.
sim <- simulate_briefrc_data(n_per_condition = 10, n_trials = 60, seed = 1)
res <- ci_from_responses_briefrc(
  sim$data,
  noise_matrix = sim$noise_matrix,
  base_image   = sim$base_face
)
dim(res$signal_matrix)   # n_pixels x n_producers
rel_split_half(res$signal_matrix, n_permutations = 200L, seed = 1)
}
}
\references{
Schmitz, M., Rougier, M., & Yzerbyt, V. (2024). Introducing the
brief reverse correlation: an improved tool to assess visual
representations. \emph{European Journal of Social Psychology}.
\doi{10.1002/ejsp.3100}
}
\seealso{
\code{\link[=ci_from_responses_2ifc]{ci_from_responses_2ifc()}}, \code{\link[=run_reliability]{run_reliability()}},
\code{\link[=run_discriminability]{run_discriminability()}}, \code{\link[=group_ci]{group_ci()}} (stage 2, optional)
}
