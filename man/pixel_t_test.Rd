% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pixel_t_test.R
\name{pixel_t_test}
\alias{pixel_t_test}
\title{Vectorised pixel-wise t-test (independent or paired)}
\usage{
pixel_t_test(
  signal_matrix_a,
  signal_matrix_b,
  paired = FALSE,
  mask = NULL,
  acknowledge_scaling = FALSE
)
}
\arguments{
\item{signal_matrix_a, signal_matrix_b}{Pixels x participants,
base-subtracted. Row counts must match. When \code{paired = TRUE}
the column counts must also match, and column names must
correspond to the same producer across matrices.}

\item{paired}{Logical. \code{FALSE} (default) uses independent
Welch t; \code{TRUE} uses paired t.}

\item{mask}{Optional logical vector of length
\code{nrow(signal_matrix_a)}. Both matrices are subsetted with the
same mask before computing t. The returned vector is then of
length \code{sum(mask)}.}

\item{acknowledge_scaling}{Logical. When \code{FALSE} (default), the
shared \code{assert_raw_signal()} helper errors on a known-rendered
matrix. Cascades to internal \code{pixel_t_test()} calls from
\code{\link[=rel_cluster_test]{rel_cluster_test()}}.}
}
\value{
Numeric vector of length \code{nrow(signal_matrix_a)} (or
\code{sum(mask)} if \code{mask} is supplied). Pixels with zero variance
get \code{0} rather than \code{NaN}.
}
\description{
At every pixel, tests whether condition A's mean signal differs
from condition B's. Two modes:
\itemize{
\item \code{paired = FALSE} (default): independent-samples Welch t per
pixel. Correct when producers in A and B are different people
(between-subjects design).
\item \code{paired = TRUE}: paired t per pixel on the per-producer
difference \code{A - B}. Correct when the same producers
contributed to both conditions (within-subjects design).
}

Returns a numeric vector of t-values, length \code{n_pixels}. Used as
an intermediate by \code{\link[=rel_cluster_test]{rel_cluster_test()}}; not intended as a
standalone inferential test.
}
\section{Reliability metrics expect raw masks}{

Welch t and paired t are variance-based and sensitive to
scaling. Inputs with \code{attr(., "source") == "rendered"} (set
automatically by Mode 1 readers like \code{\link[=extract_signal]{extract_signal()}}) error
unless \code{acknowledge_scaling = TRUE}.
}

\examples{
\dontrun{
# In a real pipeline, the two matrices come from earlier steps:
#   sig_a <- ci_from_responses_briefrc(responses_cond_a, ...)$signal_matrix
#   sig_b <- ci_from_responses_briefrc(responses_cond_b, ...)$signal_matrix
# For a self-contained demo we fabricate two small synthetic inputs:
n_pix  <- 32L * 32L
n_prod <- 20L
set.seed(1)
signal_matrix_a <- matrix(rnorm(n_pix * n_prod), n_pix, n_prod)
signal_matrix_b <- matrix(rnorm(n_pix * n_prod), n_pix, n_prod)

t_vec <- pixel_t_test(signal_matrix_a, signal_matrix_b)
summary(t_vec)
}
}
\seealso{
\code{\link[=rel_cluster_test]{rel_cluster_test()}}
}
