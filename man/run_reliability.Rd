% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_reliability.R
\name{run_reliability}
\alias{run_reliability}
\title{Run every within-condition reliability metric}
\usage{
run_reliability(
  signal_matrix,
  n_permutations = 2000L,
  null = c("none", "permutation", "random_responders"),
  noise_matrix = NULL,
  icc_variants = c("3_1", "3_k"),
  mask = NULL,
  seed = NULL,
  progress = TRUE,
  acknowledge_scaling = FALSE
)
}
\arguments{
\item{signal_matrix}{Pixels x participants, base-subtracted.}

\item{n_permutations}{Passed to \code{\link[=rel_split_half]{rel_split_half()}}. Default 2000.}

\item{null}{Forwarded to \code{\link[=rel_split_half]{rel_split_half()}}. Default \code{"none"}.}

\item{noise_matrix}{Required when
\code{null = "random_responders"}; forwarded to \code{\link[=rel_split_half]{rel_split_half()}}.}

\item{icc_variants}{Passed to \code{\link[=rel_icc]{rel_icc()}}.}

\item{mask}{Optional logical vector of length
\code{nrow(signal_matrix)}. Threaded through to both metrics.}

\item{seed}{Optional integer; used for the split-half
permutations.}

\item{progress}{Show \code{cli} progress bars.}

\item{acknowledge_scaling}{Logical. Forwarded to \code{\link[=rel_icc]{rel_icc()}}.}
}
\value{
Object of class \code{rcisignal_rel_report} with \verb{$results}
= named list of two result objects (\code{split_half}, \code{icc})
and \verb{$method = "reliability"}.
}
\description{
Convenience orchestrator that runs the reliability metrics
proper (split-half with Spearman-Brown correction and ICC) on a
single condition's signal matrix and wraps the two results in
an \code{rcisignal_rel_report} for joint printing and plotting.

Use this when you want the full within-condition reliability
report in one call. Pass each metric individually if you need
to tune arguments per metric.
}
\section{What is included (and what is not)}{

Returns the two metrics that quantify the reliability of the
group-level classification image proper: split-half (a
permutation-based estimate of group-CI stability with
Spearman-Brown projection to the full sample) and ICC(3,*) (the
psychometric variance decomposition). These are non-redundant.

Leave-one-out influence screening lives in \code{\link[=rel_loo]{rel_loo()}} and is
\strong{not} bundled here. Its output is an influence diagnostic,
not a reliability statistic, and mixing it into the reliability
report invites mis-reading \code{r_loo} values (which are near 1 by
construction) as reliability.
}

\section{Reading the result}{

\verb{$results$split_half}, \verb{$results$icc}, one result object each,
with the same fields as the standalone functions.
\verb{$method = "reliability"}.
}

\section{Reliability metrics expect raw masks}{

Both downstream metrics expect the raw mask. Inputs with
\code{attr(., "source") == "rendered"} (set automatically by Mode 1
readers like \code{\link[=extract_signal]{extract_signal()}}) error in \code{\link[=rel_icc]{rel_icc()}} unless
\code{acknowledge_scaling = TRUE}.
}

\examples{
\dontrun{
# End-to-end demo: simulate -> compute CIs -> assess reliability.
sim <- simulate_briefrc_data(n_per_condition = 10, n_trials = 60, seed = 1)
cis <- ci_from_responses_briefrc(sim$data, noise_matrix = sim$noise_matrix)
rel <- run_reliability(cis$signal_matrix,
                       n_permutations = 200L, seed = 1)
print(rel)
}
}
\seealso{
\code{\link[=rel_split_half]{rel_split_half()}}, \code{\link[=rel_icc]{rel_icc()}}, \code{\link[=rel_loo]{rel_loo()}} for
the influence diagnostic; \code{\link[=run_discriminability]{run_discriminability()}}.
}
