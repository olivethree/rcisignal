% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_rt.R
\name{check_rt}
\alias{check_rt}
\title{Response-time distributional checks}
\usage{
check_rt(
  responses,
  method = c("2ifc", "briefrc"),
  col_participant = "participant_id",
  col_rt = "rt",
  fast_threshold = 200,
  slow_threshold = 5000,
  max_fast_frac = 0.05,
  max_slow_frac = 0.05,
  min_cv = 0.1,
  ...
)
}
\arguments{
\item{responses}{A data frame of trial-level responses.}

\item{method}{Either \code{"2ifc"} or \code{"briefrc"}. Currently informational.}

\item{col_participant}{Participant id column.}

\item{col_rt}{RT column (required; the check aborts if \code{NULL}).}

\item{fast_threshold}{Milliseconds. Trials faster than this count as
"fast". Default \code{200}.}

\item{slow_threshold}{Milliseconds. Trials slower than this count as
"slow". Default \code{5000}.}

\item{max_fast_frac}{Fraction. Participants with more than this fraction
of fast trials are flagged. Default \code{0.05} (warn) / \code{0.15} (fail).}

\item{max_slow_frac}{Fraction. Participants with more than this fraction
of slow trials are flagged. Default \code{0.05} (warn).}

\item{min_cv}{Numeric. Participants with coefficient of variation of RT
below this are flagged. Default \code{0.10}.}

\item{...}{Unused.}
}
\value{
An \code{\link[=rcisignal_diag_result]{rcisignal_diag_result()}} object. \code{data$per_participant} has one row
per participant with columns: \code{n_trials}, \code{mean_rt}, \code{median_rt},
\code{sd_rt}, \code{cv_rt}, \code{pct_fast}, \code{pct_slow}, \code{is_flagged}.
}
\description{
Scans response times (RTs) for three kinds of pathology:
}
\details{
\enumerate{
\item Implausibly fast responses (\code{rt < fast_threshold}, default 200 ms).
These usually indicate the participant clicked without reading the
stimulus.
\item Implausibly slow responses (\code{rt > slow_threshold}, default 5000 ms).
These suggest distraction or a task pause.
\item Abnormally low within-participant RT variability (coefficient of
variation below \code{min_cv}, default 0.10). A near-constant RT is a hint
that the participant is responding mechanically.
}
}
\examples{
set.seed(1)
responses <- data.frame(
  participant_id = rep(1:3, each = 100),
  stimulus       = rep(1:100, 3),
  response       = sample(c(-1, 1), 300, replace = TRUE),
  rt             = c(
    exp(rnorm(100, 6.7, 0.4)) + 100,   # normal
    rep(150, 100),                     # all fast and constant
    exp(rnorm(100, 6.7, 0.4)) + 100
  )
)
check_rt(responses, col_rt = "rt")

}
