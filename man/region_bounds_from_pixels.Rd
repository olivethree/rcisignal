% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shift_mask.R
\name{region_bounds_from_pixels}
\alias{region_bounds_from_pixels}
\title{Convert pixel-coordinate rectangle bounds to 0-1 image fractions}
\usage{
region_bounds_from_pixels(row_min, row_max, col_min, col_max, img_dims)
}
\arguments{
\item{row_min, row_max, col_min, col_max}{Integer pixel
coordinates of the rectangle edges (1-indexed, with row 1 at
the top of the image and col 1 at the left). Must satisfy
\code{row_min <= row_max} and \code{col_min <= col_max}, and all four
values must lie inside the image.}

\item{img_dims}{Integer \code{c(nrow, ncol)} of the target image
(or a single integer for a square image).}
}
\value{
Numeric length-4 vector
\code{c(row_min, row_max, col_min, col_max)} in 0-1 image
fractions.
}
\description{
Bridge between visual inspection of a base image (where people
naturally think in pixel rows and columns) and the
\code{region_bounds} argument of \code{\link[=make_face_mask]{make_face_mask()}}, which expects
0-1 image fractions. Returns a length-4 numeric vector ready to
pass straight to \code{make_face_mask(region_bounds = ...)}.
}
\examples{
# On a 256x256 base, "rows 110-140, cols 60-115" becomes:
region_bounds_from_pixels(
  row_min = 110, row_max = 140,
  col_min = 60,  col_max = 115,
  img_dims = c(256L, 256L)
)

# Pass straight to make_face_mask():
bounds <- region_bounds_from_pixels(110, 140, 60, 115,
                                    img_dims = 256L)
m <- make_face_mask(c(256L, 256L), region = "left_eye",
                    region_bounds = bounds)
mean(m)
}
\seealso{
\code{\link[=make_face_mask]{make_face_mask()}}.
}
