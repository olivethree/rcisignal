# Project-specific
temp/
CLAUDE.md
.claude/

# R / RStudio
.Rproj.user/
.Rhistory
.RData
.Ruserdata
*.Rproj.user

# Build outputs
docs/
inst/doc/
*.Rcheck/
*.tar.gz

# OS
.DS_Store
/doc/
/Meta/

# Knit artifacts
vignettes/*.html

# rcicr's computeInfoVal2IFC() uses bare write() which dumps status
# strings to a file literally named "data" (R's write() default).
# Ignored so the simulate-diagnostic-chain regression test does not
# leave a dirty working tree.
tests/testthat/data

# Precompute intermediate (regenerated by data-raw/oliveira_2019/precompute.R)
vignettes/precomputed/oliveira_2019/signal_matrices.rds
docs
vignettes/figures/face_masks/_backup_pre_alignment/
